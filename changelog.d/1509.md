### Fixed — README supported-hardware table listed E1M-X V2N / V2N-M1 OS targets as "Yocto" only, omitting the committed Zephyr M33 board trees (#1509)

Both E1M-X families declare a `topology.m33_sm` slice with `toolchain: arm-zephyr-eabi` in their
SoM presets, ship a committed Zephyr board tree (`zephyr/boards/alp/e1m_v2n101_m33_sm`,
`e1m_v2m101_m33_sm`), and are walked through as a `tan build`/`tan flash` target in
`docs/getting-started.md` §7 — but the README's OS-targets column read `Yocto` only, the same
omission the E1M-i.MX93 row (correctly `Yocto + Zephyr`) doesn't have. Changed both E1M-X V2N and
E1M-X V2N-M1 cells to `Yocto (A55) · Zephyr (M33 system manager)`.
