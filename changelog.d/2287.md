### Added — Sony IMX296 Zephyr sensor driver + Raspberry Pi Global Shutter Camera shield; Stage A raw capture bench-verified (#2287)

`zephyr/drivers/video/imx296.c` adds a Sony IMX296 driver for the
INNO-MAKER CAM-IMX296RAW-TRIGGER-compatible module (IMX296LQR-C colour
part), authored from the Sony IMX296 datasheet — no permissive-licence
IMX296 driver exists anywhere to consume (not in upstream Zephyr v4.4.1,
hal_alif, or Espressif esp_cam_sensor; Linux's and libcamera's are
GPL/LGPL, not consumable under this project's licence). Linux's imx296.c
was used strictly as a REGISTER-NAME reference for a handful of names this
datasheet leaves unnamed (e.g. SENSOR_INFO, `0x3148`) — no code, text or
table from it was copied into this driver. ADR-0017-ADJACENT. All-pixel
scan mode only: 1456x1088 `SRGGB10P` RAW10 (the sensor's whole transmitted
frame — 1440x1080 is the datasheet's "recording pixels" crop inside it),
**1 MIPI CSI-2 data lane** (unlike the sibling OV5647/OV9281 shields, both
2-lane), 60.3 frame/s (datasheet mode rate; not measured), 37.125/54/74.25 MHz INCK (any other frequency
rejected at runtime with `-ENOTSUP`). Controls: exposure (lines of
integration via `SHS`), analogue+digital gain (0.1 dB step via `GAIN`), H/V
flip, pixel rate (118.8 Mpix/s), link frequency (594 MHz).

The `raspberry_pi_global_shutter_camera` shield wires the sensor's 54 MHz
`fixed-clock`, CCI address 0x1A, and single-lane endpoint onto the same
board-agnostic `raspberry_pi_camera_module_2`-style label contract as its
siblings (`chosen zephyr,camera`, `&csi_interface`, `&csi_ep_in`,
`&csi_i2c`); build with
`SHIELD="<carrier-connector-shield> raspberry_pi_global_shutter_camera"`.
`examples/aen/aen-camera-firstlight` gets a matching IMX296 `#elif` +
`testcase.yaml` scenario — see `docs/camera-shields.md` for the full
driver/shield/control reference and bench-status split.

**Stage A scope, bench-verified on E1M-AEN803 2026W36-0001 (E1M-EVK,
`csi_i2c` = I2C1 @ `0x49011000`)**: I2C identity (bench run 229 — CCI
`0x1A`; `STANDBY`/`XMSTA` match datasheet POR defaults; SENSOR_INFO
register `0x3148`/`0x3149` reads `0x4A00` — **undocumented in the public
preliminary datasheet**, so treated as an opaque bench-confirmed signature
for the colour LQR variant, not a documented chip-ID register, and gates
`imx296_init()`), CSI-2 streaming, and a full 1456x1088 RAW10 frame
captured through `<alp/camera.h>` and verified against a real scene: bench
run 292 (single capture, no discard, evening light, gain 0) reads mean
pixel value 61.19, max 108, a clean close-out (no crash, no error return)
and no mod-4-column RAW10 byte-phase-slip pattern in the captured image;
its 8x8-block correlation against diag control run 293 (`SHS`=14 written
directly, same light) is 0.98. Both runs are still dim — about 1 code
above black (60) under this bench's evening light — so this confirms the
register writes and capture pipeline now agree and produce a real (if
faint) image, not yet a well-exposed one; better lighting or exposure
tuning is a follow-up. **Run 292's build had `CONFIG_LOG` unset (no log
output at all)**, so nothing about its console — clean or otherwise — says
anything about `INT_IPI_PIXEL_IF_HLINE_ERR` or
`INT_IPI_PIXEL_IF_FIFO_OVERFLOW`: their status on the product build is
UNKNOWN, not "not seen". The `37-80` per-capture and `~10.8k` HLINE_ERR
counts elsewhere in this changelog were measured in EARLIER diag runs (the
269-276-era diag-instrumented builds, and run 285's 16-frame continuous
diag run, which DID have logging), all predating the init-period-wait,
SHS, and CPI-only-pause fixes below. Continuous streaming has not been
benched on the product (non-diag, logging-enabled) build at all, so
whether HLINE_ERR/FIFO_OVERFLOW occur there, post-fix, remains open.

**NOT yet bench-verified, tracked for a later stage**: the ISP-Pico colour
path, auto-exposure, `camera-mjpeg-stream`-style continuous streaming
beyond the single-capture/starvation-paced loop `aen-camera-firstlight`
exercises, and fast-trigger mode (added below, but unverified — see its
own caveats). Whether a paused 2-lane sensor (OV5647, OV9281 — this CPI's
other sensors) can back-pressure the IPI into overflow during a longer
starvation pause is also untested.

`tests/zephyr/video_sensors` adds a real runtime ztest suite
(`imx296_test.c`) against its own I2C emulator (`imx296_emul.c`), cloning
the OV5647/OV9281 emulator+ztest shape: the SENSOR_INFO identity probe
(accept and `-ENODEV` reject paths), the STANDBY/XMSTA/init register-write
ORDER (not just final values), the exact init register values (`VMAX`
1118, `HMAX` 1100, `INCKSEL0..3`, `0x418C`, `BLKLEVEL`, ROI-disable,
`0x3005`, `SHS`/`GAIN`/`REVERSE` defaults), the one fixed format, and the
exposure/gain/flip control paths.

#### Fast-trigger exposure mode (external trigger) — added, UNVERIFIED

`imx296.c` also adds a driver-private control, `IMX296_CID_TRIGGER_MODE`
(`VIDEO_CID_PRIVATE_BASE + 0x01`, default free-run — unchanged behaviour),
switching the sensor into the datasheet's Global Shutter **Fast Trigger
Mode** (`TRIGEN` at `0x300B`, `LOWLAGTRG` at `0x30AE`, `SYNCSEL` at
`0x3036` — "Global Shutter (Fast Trigger Mode) Operation", page 64). This
is the only one of the datasheet's two trigger sub-modes this hardware can
reach: Sequential Trigger Mode is "slave mode only" (page 62) and, like
every sensor on the RPi-style 15-pin CSI connector, this module has no
XVS/XHS lines wired, so slave mode is never reachable. Trigger mode is
still MASTER mode, started by the same `XMSTA` write as free-run; only
frame output and exposure timing become XTRIG-driven afterward. The mode
switch is applied in `imx296_set_stream()`, before `STANDBY` is cancelled,
since "Mode Transitions of Global Shutter Operation" (page 66) requires
it. Setting the control while already streaming is rejected `-EBUSY`.

`examples/aen/aen-camera-firstlight` gets an opt-in `-DAEN_CAMERA_TRIGGER=ON`
CMake option (`CMakeLists.txt`, `trigger_gpio.overlay`): arms the
control and pulses a GPIO on Alif P5_1 (Arduino D4 / `EVK_PIN_CK_DIO4`,
E1M pad M2) to capture and timestamp a few frames, for the sensor module's
own opto-isolated J3 Trig+ header (on the INNO-MAKER module, not the
E1M-EVK carrier). **Trigger electrical polarity is UNVERIFIED** — the
INNO-MAKER module's J3 input circuit has not been checked against that
module's own documentation, so the DT `GPIO_ACTIVE_HIGH`/`GPIO_ACTIVE_LOW`
flag on `imx296-trigger-gpios` is a guess — **do not wire J3 until that
circuit is confirmed**. **UNTESTED ON SILICON**: both the driver's trigger
mode and this example's build mode compile clean (0 warnings) but have not
run with a trigger pulse, and the 150 ms sensor init-period wait added
below (`IMX296_INIT_PERIOD_MS`) covers free-run timing only — trigger-mode
init timing is unhandled and unbenched.

### Fixed — bring-up bugs found and fixed before Stage A shipped (#2287)

- **LP-11 clock-lane park.** `imx296_init()` leaves the sensor in STANDBY,
  and this datasheet documents no register that forces the CSI-2 CLOCK
  lane to Stop-state (LP-11) short of exiting STANDBY into full
  master-mode streaming — the same shape OV5647 hit (#2248), fixed there
  with an explicit lane-park sequence this datasheet gives no equivalent
  registers for. An earlier version of this fix made the D-PHY's
  Stop-state wait (`dphy_dw_slave_setup()`) warn-and-continue on ANY
  timeout, for every sensor; that was wrong (it would have silently
  re-opened the exact class of bug that same wait caught for OV5647,
  #2248) and is reverted. Fixed narrowly instead: `struct
  dphy_csi2_settings` gains a `skip_clk_lane_stopstate` field, set only
  from `raspberry_pi_global_shutter_camera.overlay`'s
  `no-lp11-clock-lane-park` DT property, skipping ONLY the clock-lane
  Stop-state bit for this one sensor; every data-lane bit and the wait's
  timeout stay fatal for this sensor and every other.

- **CSI-2 data lane stuck in LP-11 without a bench-derived register
  write.** Without writing register `0x3005 = 0xF0` (undocumented in the
  datasheet; bench runs #263/#264 found it by sweep, not from the
  register map) the data lane never left LP-11 at all — no HS output.
  `imx296_init()` now writes it unconditionally, documented in-code as
  bench-derived. Also added, datasheet-cited this time (pages 49, 51-52):
  `BLKLEVEL` (`0x3254`, the datasheet's recommended `0x03C`) and the
  `FID0_ROIH1ON`/`FID0_ROIV1ON` pair (`0x3300`, disabled) — the latter is
  this datasheet's actual all-pixel-vs-ROI-mode switch, written explicitly
  so a prior ROI-mode session's crop window can't leak across a warm SoC
  reset (the module stays powered across one).

- **IPI-fatal log/reset flood capped.** `video_csi_dw.c`'s `csi2_dw_irq()`
  used to `LOG_ERR` and IPI-soft-reset on every single
  `CSI_INT_ST_MAIN_IPI_FATAL` event, uncapped — with bad IPI framing this
  could fire once per line, flooding the log and storming the IPI reset
  line. It now logs only the first 16 occurrences per unmask window
  (`CSI2_DW_IPI_FATAL_LOG_LIMIT`), then masks `CSI_INT_MSK_IPI_FATAL` and
  logs one "masked after 16" line; the window resets via
  `csi2_dw_irq_on()`, which now also runs from `csi2_dw_stream_start()` on
  every user stream (re)start, not just from `csi2_dw_configure()` — so a
  mask a previous stream left set no longer hides every fatal on the next
  one. A separate, never-reset `ipi_fatal_total` field keeps the lifetime
  count for diagnostics.

- **Runtime `CSI_IPI_SOFTRSTN` toggle removed from the IPI-fatal ISR.**
  Bench run 271 (product image), WITH the runtime toggle present, panicked
  at buffer close: the CPI's DMA wrote ~2744 bytes past a 3,168,256-byte
  frame buffer's end, corrupting the next heap chunk header
  (`sys_heap_free()`'s hardening check caught it). Bench runs 272-276,
  WITHOUT the toggle, all completed capture+close with no panic — 1
  overrun in 1 run with the toggle vs. 0 in 5 without it: a correlation,
  not a confirmed mechanism (run 271's own logging was disabled, so which
  IPI-fatal event fired that run is unknown). `csi2_dw_irq()` no longer
  soft-resets `CSI_IPI_SOFTRSTN` on any fatal event, matching the vendored
  Alif DFP reference's own IPI-fatal handler, which never resets IPI at
  runtime either. This hazard is not IMX296-specific in principle —
  OV5647/OV9281 use the same zero-slack frame buffers and are **likely
  exposed** but this has **not been bench-reproduced** on either.

- **DW CSI-2 IPI switched to Controller timing mode.** The host's default
  "Camera" IPI mode derives its line timing from the D-PHY clock and
  emitted **no** IPI lines at all for this 1-lane sensor on silicon.
  `raspberry_pi_global_shutter_camera.overlay` now switches the `&csi`
  controller to IPI **Controller** timing mode with fixed
  `csi-hsa`/`csi-hbp`/`csi-hsd`/`csi-vsa`/`csi-vbp`/`csi-vfp` timings
  derived from this sensor's own `HMAX`/`VMAX` (HSD bench sweep: 503 gives
  zero IPI-FIFO overflow; 520 and 531 both overflowed and banded the
  frame). These `ipi-mode`/timing properties live on the `&csi` controller
  node itself, not a per-sensor endpoint, so they apply to whatever sensor
  is stacked on this controller, not IMX296 specifically -- today only one
  sensor is ever wired to a given `&csi` controller, so this is a
  scope note, not a live conflict. `csi2_dw_validate_data()` also drops
  its usual 20% bandwidth margin on the CSI IPI pixel-clock request in
  Controller mode: that margin exists to keep the IPI ahead of the sensor
  in Camera mode, where HSD is *derived from* the resulting clock; in
  Controller mode the DT timings *set* the drain rate instead, and the
  margined request picked a different, starving clock divisor (bare
  request → divisor 3, 133.33 MHz, matching the overlay's derivation;
  margined request → divisor 2, 200 MHz -- **calculated from the
  clock-control divisor rule, not independently bench-measured with a
  scope/analyzer; the margined request was never actually made on the
  bench** (the fix removes it before any hardware run). This is what took
  the capture path from "emits no IPI line at all" to full 1456x1088
  frames. `csi2_dw_validate_data()` also gained a `LOG_INF` (on every
  `set_format`, Controller mode only) reporting the pixel clock actually
  programmed, the DT-fixed `HLINE`, and the line time they work out to --
  so a future clock-control divisor change that silently invalidates this
  overlay's HSD derivation shows up in the log instead of only as a
  re-appeared FIFO overflow.

- **Buffer-starvation pause STANDBY-cycled the sensor on every frame.**
  The starvation pause (`alif_cam_work_helper()`) and its resume
  (`alif_cam_enqueue()`) used to call `video_stream_stop()`/`_start()` on
  the CSI-2 endpoint, driving the sensor into STANDBY on every pause and
  back to RUN on every resume — a full standby-to-run cycle per frame with
  a one-buffer capture/release loop. The datasheet's 8-frame
  initialization period after STANDBY cancel (below) made this worse: the
  kept frame was always still ramping up. Diag run 285 (predating the
  init-period-wait fix below) also showed a RAW10 4-column byte-phase-slip
  pattern with the old endpoint-cycling behaviour, SUSPECTED (not
  confirmed) to be the CPI's frame-boundary tracking desyncing from the
  CSI-2 IPI stream on each endpoint restart. Fixed: the pause now stops
  only the CPI capture engine (`hw_cam_cpi_only_stop()` — `CAM_CTRL` stop,
  `BUSY` poll, soft reset, clear latched interrupts, the same steps
  `alif_cam_stream_stop()` applies to the CPI side of its own teardown)
  and leaves the CSI-2 endpoint and sensor streaming across the stall; the
  resume path restarts only the CPI. A user `stop()`/`close()` is
  unaffected — `alif_cam_stream_stop()` still calls `video_stream_stop()`
  on the endpoint unconditionally, the only stop that ever reaches it
  while a starvation pause is in effect. While paused, the sensor, D-PHY
  and CSI-2 host keep running (and drawing power); frames the sensor sends
  during the pause are simply dropped at the stopped CPI. `hw_cam_cpi_only_stop()`
  lives in the shared CPI driver (`video_alif.c`), so this behaviour change
  applies to OV5647 and OV9281 too, not just IMX296 -- but it is
  bench-verified on IMX296 only; whether a paused 2-lane sensor can
  back-pressure the IPI into overflow during a longer pause is untested,
  and re-bench on OV5647/OV9281 is pending.

- **First frame after every stream start was inside the 8-frame init
  period.** "Standby mode" (datasheet p54): a normal image is output only
  from the 9th frame after STANDBY cancel. `imx296_set_stream(dev, true,
  ...)` used to return as soon as the `XMSTA` write started master-mode
  free-run, so the CPI's own capture-snapshot arm
  (`alif_cam_stream_start()` → `hw_cam_start_video_capture()`, called
  immediately once the sensor's `set_stream` returns all the way back up
  through `csi2_dw_stream_start()`) could land inside that init period.
  Fixed in the sensor driver, not the apps that tripped over it:
  `imx296_set_stream()` now blocks for `IMX296_INIT_PERIOD_MS` (150 ms, 9
  frames of margin over the 132 ms the 8-frame period works out to at
  `IMX296_VMAX` x 1H = 1118 x 14.81us, p44/p53) after the `XMSTA` write,
  before returning — free-run mode only (see the trigger-mode caveat
  above).

- **SHS init-write bug: an accidental 14-line exposure.**
  `imx296_init()`'s hardware-defaults write wrote `IMX296_REG_SHS =
  IMX296_VMAX - IMX296_SHS_DEFAULT` (1104), wrongly applying the
  exposure-lines-to-SHS conversion `imx296_set_ctrl()` uses for control
  values. SHS is the shutter START line, not exposure lines
  ("Calculation Formula of Exposure Time", page 60): the register value
  should be `IMX296_SHS_DEFAULT` (14) itself, giving the intended
  1104-line exposure — not `IMX296_VMAX` minus it. The wrong write put
  SHS=1104 in hardware, a 14-line (~0.2 ms) exposure instead of 1104
  lines — this, not "no light reaching the array", is why every product
  capture before this fix read flat black (mean pixel value 60.15,
  bench runs 288/291): `imx296_set_ctrl()`'s own conversion was already
  correct and is unchanged. Fixed to write `IMX296_SHS_DEFAULT` directly.
  `test_shs_gain_reverse_defaults_written_at_init` asserted the old,
  wrong register value; it now asserts the correct one. Confirmed fixed
  together with the init-period-wait fix above by bench run 292 (see the
  Stage A summary above).

`tests/zephyr/video_sensors` only emulates the IMX296 sensor over I2C/CCI
and never instantiates `video_csi_dw.c` or a clock-control device, so
there is no native_sim harness the DW CSI-2 IPI-timing-mode change above
can be asserted against; no test was added for it.

### Added — IMX296 ROI mode (1280x960) + DW CSI-2 controller-timing that follows any format; bench-verified (#2287, Stage B)

`zephyr/drivers/video/imx296.c` gains a second `video_format_cap`: a
centred 1280x960 `SRGGB10P` crop within the same 1456x1088 full-pixel
array, alongside the existing full-frame mode (`video_set_format()` now
accepts either). "ROI mode" / "Register List of ROI mode" / "Restrictions
on ROI mode" (Sony IMX296 datasheet pp.51-53): `FID0_ROIH1ON`/`ROIV1ON`
(`0x3300`, bit0/bit1) enable the crop; `FID0_ROIPH1`/`ROIPV1`/`ROIWH1`/
`ROIWV1` (`0x3310`/`0x3312`/`0x3314`/`0x3316`, all 16-bit LE) set its
position (88, 64 — centred, both already a multiple of 4) and size (1280,
960 — 1280 ≥ the 80-pixel minimum, 960 ≥ the 4-line minimum, both
multiples of 4). `VMAX` stays the driver's fixed 1118 for both modes
(unchanged by this change) — the datasheet's ROI frame-rate floor is
`VMAX ≥ ROIWV1 + 30 = 990`, so 1118 clears it with margin and the crop
keeps the same 60.3 frame/s the full-frame mode already has, not a
faster rate. `imx296_set_stream()` now writes the position/size registers
and the enable bits (or the disable value, unchanged behaviour) every
stream start, reading back whatever format `video_set_format()` last
selected — full-frame mode is unaffected (still writes `ROI_ENABLE`
disable, now explicitly on every start rather than only once at boot).
Emulator coverage added to `tests/zephyr/video_sensors/src/imx296_test.c`:
`format_caps` now lists both modes, the ROI registers are asserted written
for the 1280x960 format, `ROI_ENABLE` is asserted disabled for the
1456x1088 format, and a size between the two fixed modes is asserted
rejected (`-ENOTSUP`).

`zephyr/drivers/video/video_csi_dw.c` gains two optional DT properties on
the DW CSI-2 IPI controller node, `csi-hline` and `csi-vtotal`
(`zephyr/dts/bindings/video/snps,designware-csi.yaml`): when both are set,
`csi2_dw_validate_data()` DERIVES `hsd`/`vfp` from them and the CURRENTLY
selected format's `hact`/`vact` (`hsd = csi-hline - hsa - hbp - hact`,
`vfp = csi-vtotal - vsa - vbp - vact`, rejecting a negative result with
`-EINVAL`) instead of reading a fixed `csi-hsd`/`csi-vfp` DT value. A fixed
`csi-hsd` is only ever correct for the one `hact` it was bench-swept
against — with IMX296 now offering a second, narrower ROI mode, the fixed
`503` the shield overlay carried would make a 1280-wide capture's IPI line
9% shorter than the sensor's own line time, starving the IPI rather than
draining it. `raspberry_pi_global_shutter_camera.overlay` now sets
`csi-hline = <1972>` and `csi-vtotal = <1118>` in place of the fixed
`csi-hsd = <503>` / `csi-vfp = <25>` it used to carry; the overlay's own
comment re-derives both full-frame values from the new formula —
`hsd = 1972 - 3 - 10 - 1456 = 503`, `vfp = 1118 - 1 - 4 - 1088 = 25` —
and they land EXACTLY on the bench-swept fixed values Stage A verified, so
this change is not expected to alter full-frame behaviour on real
silicon. Boards that don't set `csi-hline`/`csi-vtotal` keep the old fixed
`csi-hsd`/`csi-vfp` code path byte-for-byte. As with the Stage A DW CSI-2
change above, `tests/zephyr/video_sensors` has no `video_csi_dw.c` /
clock-control harness to assert this derivation against; no test was
added for it.

`examples/aen/aen-camera-firstlight` gains an opt-in
`-DAEN_CAMERA_IMX296_ROI=ON` CMake option (default `OFF`, unchanged
1456x1088 full-frame build otherwise) selecting the 1280x960 ROI crop, plus
a matching `build_only` `testcase.yaml` scenario
(`camera_firstlight.imx296_roi`).

**Bench-verified, runs 294/295 (E1M-AEN803 2026W36-0001, logging on)**:
run 294 (full-frame, unchanged mode) captured a
clean 3,168,256-byte frame; run 295 (1280x960 ROI) captured a clean
2,457,600-byte frame — both capture and close without error. Run 295's
frame matches an 88,64-offset crop of run 294's (8x8-block correlation
0.91), confirming `IMX296_ROI_POS_H`/`POS_V` (88/64) land where derived,
not just that the driver builds. Neither frame shows the mod-4-column
RAW10 byte-phase-slip pattern Stage A's bring-up used as its own
CSI-2-desync tell (both runs were dim, evening light, gain 0 — expected,
not itself evidence of anything wrong). Console (both runs): `"CSI IPI
Controller-mode: pixclk 133333333 Hz, HLINE 1972 px -> line time 14790
ns"`, matching `raspberry_pi_global_shutter_camera.overlay`'s own
`csi-hline`/`csi-vtotal` derivation exactly. The console ALSO shows 16x
`"Fatal Interrupt at IPI interface. status - 0x10"` (`HLINE_ERR`, per
`video_csi_dw.h`) during `open`/`start`, then `"IPI fatal interrupts
masked after 16"` — `HLINE_ERR` DOES fire at stream start on the product
build, on both the unchanged full-frame mode and the new ROI mode (so
this is not new to Stage B); the count after masking is UNKNOWN
(`csi2_dw_irq()`'s `CSI2_DW_IPI_FATAL_LOG_LIMIT` masking, `video_csi_dw.c`,
stops logging past 16 events — see that mechanism's own comment). Both
frames still arrived clean despite these transient start-of-stream
errors — root cause not yet investigated, filed as a follow-up, not a
regression this Stage B change introduced (Stage A's own runs never had
`CONFIG_LOG` on to have shown it either way, see the Stage A summary
above).

A derived `hsd`/`vfp` that exceeds `CSI_IPI_HSD_TIME_MASK`/
`CSI_IPI_VFP_LINES_MASK` (12/10 bits) is now rejected with `-EINVAL`
instead of being silently truncated into a wrong in-range value by
`csi2_dw_ipi_set_timings()`'s own register write. A `BUILD_ASSERT` in
`ALIF_MIPI_CSI_DEVICE()` catches `csi-hline` set without `csi-vtotal` (or
vice versa) at compile time; it does NOT also assert the fixed
`csi-hsd`/`csi-vfp` properties are absent when `csi-hline`/`csi-vtotal`
are set, because `ensemble_e8_peripherals.dtsi`'s shared `&csi` node sets
both unconditionally for every board — `csi-hsd`/`csi-vfp` are simply
ignored (overridden by the derivation) when `csi-hline`/`csi-vtotal` are
present, not absent.

Page 53's `ROIWV1 + 30` is the MAX-RATE setting for a given `ROIWV1` (the
smallest legal `VMAX`, hence the fastest frame rate that `ROIWV1` can
reach) — keeping `VMAX` at the driver's existing fixed 1118 instead is an
inference, not something the datasheet requires; run 295 confirms the
crop itself is correct at that `VMAX` (a clean 1280x960 frame, no
byte-phase-slip), but neither run 294 nor 295 measured frame RATE, so the
60.3 frame/s figure IMX296's other constants document is still
datasheet-derived, not bench-measured, for ROI mode. `imx296_set_stream()`'s
ROI-write comment also cites datasheet page 51's "one invalid frame when
the ROI area changes" note: the existing `IMX296_INIT_PERIOD_MS` wait
already covers it, since every full-frame/ROI switch goes through a
stop/change-format/start sequence with the sensor in STANDBY, never a
live switch while streaming.

### Added — IMX296 SRGGB10P through the ISP-Pico pipeline; bench-verified data path, image quality not (#2287, Stage B)

`zephyr/drivers/video/isp_pico.c`'s `supported_input_fmts[]` (the ISP
driver's own INPUT format gate) gains a `VIDEO_PIX_FMT_SRGGB10P` entry —
the fourcc IMX296's driver (`zephyr/drivers/video/imx296.c`) advertises for
its one fixed all-pixel mode and its new ROI crop alike. Without this
entry `find_format()` rejected the fourcc with `-ENOTSUP` before the ISP
ever saw it, the same gap `VIDEO_PIX_FMT_SBGGR10P` had for OV5647 before
this branch's earlier bring-up added it. `bayer_sample_depth()` (also in
`isp_pico.c`) already had a `VIDEO_PIX_FMT_SRGGB10P` case (10-bit
`PIN_MAPPING`) predating this change — only the input-format cap entry was
missing.

`src/backends/camera/alif_isp_pico.c`'s `ALIF_ISP_INPUT_FOURCC` — until now
a single fixed `VIDEO_PIX_FMT_SBGGR10P` for the OV5647 board this backend
was bench-proven against — is now a compile-time choice:
`DT_HAS_COMPAT_STATUS_OKAY(sony_imx296) ? VIDEO_PIX_FMT_SRGGB10P :
VIDEO_PIX_FMT_SBGGR10P`. Deliberately NOT a runtime `video_get_format()`
probe of the ISP input device: that call is a `alif_cam_set_fmt()`
(`video_alif.c`) SIDE EFFECT, and that call fails outright at IMX296's
1088-tall full-frame height — probing before this backend has even chosen
a fourcc would break it on IMX296 before `ALIF_ISP_INPUT_FOURCC` ever
mattered.

New `zephyr/patches/hal_alif/0012-isp-srggb10p-input.patch` (registered in
`zephyr/patches.yml`, `module: alif`) adds `VIDEO_PIX_FMT_SRGGB10P` to
`isp_api_wrapper.c`'s `isp_mode_from_fourcc()` (→ `ISP_MODE_RAW`, it fell
through to `ISP_MODE_BT656` before — wrong, this is RAW Bayer, not BT.656
YUV) and `isp_pixelfmt_from_fourcc()` (→ `PIXEL_FORMAT_RGGB10`, same value
the pre-existing unpacked `VIDEO_PIX_FMT_RGGB10` case already uses — it
fell through to the 12-bit `PIXEL_FORMAT_RGGB12` default before — wrong,
IMX296 samples at 10 bits). Same shape as patch 0003's
`VIDEO_PIX_FMT_SBGGR10P` addition for OV5647's packed format; unlike
0003, which serves only OV5647's one fixed mode, this fourcc mapping
covers BOTH of IMX296's modes (its fixed full-frame mode and its ROI crop)
since both advertise the same `SRGGB10P` pixel format — only the ROI crop
actually fits through the ISP's 1080-line input-height cap (see below),
the full-frame mode's 1088 lines does not.
Built and verified against a disposable clone of the pinned `hal_alif`
`v2.3.0` release with patches 0001–0011 applied first, then this one; the
tracked `hal_alif` checkout this workspace actually builds against was
never modified.

`examples/aen/aen-isp-capture` (renamed from `aen-isp-ov5647-capture` —
`git mv`, plus every reference across the tree: the app's own
`project()`/testcase IDs/banner strings, `isp_pico.c`/`alif_isp_pico.c`/
`include/alp/camera.h`'s doc comments, `zephyr/patches.yml`,
`zephyr/kconfigs/*.kconfig`, and earlier `changelog.d/*.md` fragments that
cited its old path — the sibling `aen-isp-ov5647-viewfinder` example is
untouched, it is a different app) gains an opt-in `-DAEN_ISP_IMX296=ON`
CMake option (default `OFF`, unchanged OV5647 build otherwise) plus a
matching `build_only` `testcase.yaml` scenario (`isp_capture.imx296`,
using the `raspberry_pi_global_shutter_camera` shield +
`overlay-no-ae.conf`). OV5647 stays the example's default; the rename is
to stop implying this is an OV5647-only app now that it captures either
sensor. The variant captures IMX296's 1280x960 ROI crop (not the
1456x1088 full frame — `isp_pico.c`'s `ISP_VIDEO_FORMAT_CAP` caps every
input format at height 1080, which 1088 exceeds) through the same
`sensor -> csi -> cam -> isp` graph as OV5647 (the board overlays are
sensor-agnostic — they reference only `&cam`/`&isp`, never `&ov5647` — so
no new overlay files were needed), ONE frame only, with AE off
(`overlay-no-ae.conf`, the same file OV5647's own diagnostic uses) and
AWB set to manual (`video_set_ctrl(isp_dev, AUTO_WHITE_BALANCE, 0)`) —
there is no IMX296-fitted AWB/CCM calibration yet (patches 0008/0011's
tables are OV5647-only, gated on `CONFIG_VIDEO_ISP_VSI_CALIB_OV5647`), so
running the auto loop against the stock ARX3A0 calibration would tune
against facts that don't describe this sensor.

Bench run 296 (an earlier version of this change, still 2 buffers but no
pool resize) hit `video_buffer_aligned_alloc[1](1843200) returned NULL`:
IMX296's 1280x960 buffers are each 1,843,200 bytes, so 2 of them
(3,686,400 bytes) exceeded the example's default 2 MiB pool (sized for
OV5647's smaller 640x480 buffers). Fixed with a new
`overlay-imx296.conf`, wired in automatically by the CMake option, sizing
`CONFIG_VIDEO_BUFFER_POOL_HEAP_SIZE` to fit both buffers plus slack. The
IMX296 variant also no longer copies the kept frame into a separate
static buffer the way the OV5647 path does (that copy alone would have
been another ~1.8 MB competing for the same SRAM0 region) — since it
only ever captures one frame, it instead holds the DEQUEUED buffer
itself, printing its address, byte count and CRC32 and sleeping 20 s
*before* re-queueing it, so a bench `savebin` reads the real captured
data rather than a buffer the driver may already have started
overwriting. OV5647's own capture path (30 frames, a real copy needed
because that buffer gets re-queued and possibly rewritten before the
bench can read it) is unchanged.

A build against the fixed pool size compiles 0 warnings on both the
IMX296 variant and the unchanged OV5647 default.

Bench run 297 (`examples/aen/aen-isp-capture`, `-DAEN_ISP_IMX296=ON`, IMX296
ROI 1280x960 `SRGGB10P` → ISP-Pico → YUV420 1280x960, AE off, AWB off):
capture OK, CRC matches the host-side dump, Y-plane mean 56 (range 55–72
across the frame, evening bench-room light, gain fixed at the AE-off
floor), U mean 142 / V mean 152 (uncalibrated — AWB off, no IMX296 AWB/CCM
table exists yet, see above). The ISP's output Y plane correlates 0.83
(32x32-pixel block means) against the raw ROI frame captured on run 295
before the ISP — the data path through the ISP-Pico pipeline (input-format
cap entry, hal_alif 0012 fourcc mapping, backend's compile-time
input-fourcc choice, example's IMX296 variant) is verified end to end.
**Image quality is NOT verified**: a 0.83 correlation and uncalibrated
U/V means are consistent with the debayer/colour path running against
IMX296 data it has no calibration for, not with a validated picture —
AWB/CCM/AE tuning for this sensor is unit 3's and later work.

### Added — IMX296 AE (auto exposure/gain) envelope through the ISP-Pico pipeline; PARTIALLY BENCH-VERIFIED (#2287, Stage B unit 3)

New `CONFIG_VIDEO_ISP_VSI_CALIB_IMX296` (`zephyr/kconfigs/vendor-alif-
peripherals.kconfig`, default `y` if `VIDEO_IMX296`, mutually exclusive
with `CONFIG_VIDEO_ISP_VSI_CALIB_OV5647` via `depends on
!VIDEO_ISP_VSI_CALIB_OV5647`) selects a real IMX296 AE envelope in place
of OV5647's (units 1/2) or the stock ARX3A0 one. New hal_alif
`zephyr/patches/hal_alif/0013-isp-imx296-ae-envelope.patch` (registered in
`zephyr/patches.yml`, `module: alif`, `upstreamable: false`) adds a new
`drivers/isp/isp_wrapper/inc/imx296_ae_envelope.h` (mirrors patch 0011's
`ov5647_ae_envelope.h` — same macro shape, same `_Static_assert`
round-trip band) sourced from the Sony IMX296LQR-C datasheet alone
(pp.44, 49, 56, 60; clean-room, no other IMX296 driver or calibration
table consulted): `fullLines`/`fps` 1118 lines @ 60.3 frame/s,
`minIntLine`/`maxIntLine` — exposure LINES (`VIDEO_CID_EXPOSURE`, this
library's own unit) 1..`VMAX - 4` = 1114, NOT the sensor's `SHS` register
directly: `SHS` is inversely related (`exposure_lines = VMAX - SHS`,
p.60) and has its own, different legal range, `4 <= SHS <= VMAX - 1`
(p.60) —
`minAgain`/`maxAgain` 1024/257217 (0/48.0 dB, `GAIN` register 0-480 in
0.1 dB/count, p.56 — matches `imx296.c`'s own `IMX296_GAIN_MAX = 480`
exactly), `dgain` pinned 1024 (unity — no separate digital-gain
register, p.56). The patch adds a `#elif
defined(CONFIG_VIDEO_ISP_VSI_CALIB_IMX296)` arm alongside each existing
`#if`/`#else` in `sensor_attributes.h`'s `AE_SNS_DEFAULT_S` table (patch
0006) and `isp_param_conf.h`'s `.ae` calibration block (patch 0011) —
**not editing 0006/0008/0011 themselves**, both existing arms (OV5647
and ARX3A0) are untouched, confirmed by preprocessing
`sensor_attributes.h`/`isp_param_conf.h` with
`CONFIG_VIDEO_ISP_VSI_CALIB_OV5647=1` before/after patch 0013 and diffing
the output (byte-identical). AWB/CCM are **not** covered: both files'
AWB/CCM blocks stay gated on `CONFIG_VIDEO_ISP_VSI_CALIB_OV5647` only, so
an IMX296 build falls through to the ARX3A0 AWB/CCM defaults (compiles
and runs, does not colour-calibrate IMX296) until a future unit gives
IMX296 its own AWB/CCM table.

**IMPORTANT — SHS vs. exposure lines**: the writeback/readback throughout
this unit targets `VIDEO_CID_EXPOSURE` (a LINE count) and IMX296's own
`GAIN` register, never the sensor's `SHS` hardware register directly.
`imx296_set_ctrl()` (`zephyr/drivers/video/imx296.c`) is what converts a
line count to `SHS` before writing it — `SHS = lines_per_frame - lines`
(datasheet p.60), an INVERSE relationship, not a direct copy. Every
comment in this unit that referred to "IMX296's SHS register" as if it
were the same LINEAR unit `VIDEO_CID_EXPOSURE` uses has been corrected to
say `VIDEO_CID_EXPOSURE` instead (the diagnostic register dump in
`print_imx296_ae_regs()`, below, is the one place that genuinely reads
the raw `SHS` hardware register — that is a real, direct I2C read, not a
writeback-path abstraction).

**Unit conversion** (`isp_pico.c` becomes the owner, per its own
"sensor-specific constants in the generic ISP driver" review-round
comment): new `zephyr/drivers/video/isp_sns_gain_conv.h` — a
Zephyr/hal_alif-independent header (only `<stdint.h>`) holding a
481-entry `isp_gain_db_tenths_table[]` (`round(1024 × 10^(n/200))` for
`n` = 0..480 tenths of a dB) plus pure conversion functions: dB-tenths
register ↔ library gain units (forward = table lookup, reverse = binary
search for the closest entry by linear distance, which lands within 1
count of `round(200×log10(total/1024))`, without ever calling `log10`),
linear register ↔ library gain units (the OV5647 `× reg_per_1x / 1024`
scale, generalised), and lines → `VIDEO_CID_EXPOSURE` control units.
`isp_pico.c` wraps these in three non-static, Kconfig-dispatched
functions — `isp_sns_gain_ctrl_from_lib()`, `isp_sns_gain_lib_from_ctrl()`,
`isp_sns_exposure_ctrl_from_lines()` — selected by three new Kconfig
symbols (`CONFIG_VIDEO_ISP_VSI_SNS_GAIN_DB_TENTHS`, default `y` if
`VIDEO_ISP_VSI_CALIB_IMX296`; `CONFIG_VIDEO_ISP_VSI_SNS_GAIN_REG_PER_1X`,
default 16, deliberately **not** `depends on !...DB_TENTHS` — both
branches of the `IS_ENABLED()` dispatch are compiled either way, so a
real `depends on` there breaks every DB-tenths build with an
undeclared-identifier error; `CONFIG_VIDEO_ISP_VSI_SNS_EXPOSURE_CTRL_PER_LINE`,
default 16 / 1 if `VIDEO_ISP_VSI_CALIB_IMX296`) — all three anchored to
the calibration-selection symbol, not the raw sensor-driver enable, so
every IMX296-AE-specific default tracks one selector. Patch 0013's
`isp_api_wrapper.c` hunk replaces its old hardcoded OV5647-only
`totalGain * 16 / ISP_SNS_GAIN_ACCU` / `intLine * 16` writeback literals
(patch 0005) with `extern`-declared calls to the same two `isp_pico.c`
functions — a different translation unit in the same final link. The
conversion still runs **before** patch 0010's clamp-to-ctrl-range
(unchanged ordering — the clamp compares `cq.range`, which is in ctrl
units, so it must run after the value is already in ctrl units).

A native_sim ztest suite, `tests/zephyr/isp_ae_conv` (`alp_sdk.video.isp_ae_conv`),
includes `isp_sns_gain_conv.h` standalone (no ISP driver, no emulator, no
device tree) and covers 0 dB/6 dB/48 dB, the 0/480 register clamps, the
OV5647 linear ×16 path (bit-identical to the old hardcoded formula), the
exposure line-scaling helper for both sensors, and a 481-entry
monotonic + round-trip sweep of the whole `isp_gain_db_tenths_table[]` —
all pass (`zephyr.exe` run locally).

`examples/aen/aen-isp-capture`'s existing `-DAEN_ISP_IMX296=ON` IMX296
variant gains an AE-on scenario: with `CONFIG_ISP_LIB_AE_MODULE=y` (this
example's own prj.conf default — AE-off is the OPT-IN variant, via
layering `overlay-no-ae.conf`), `N_FRAMES` becomes 60 (was 1
unconditionally, the AE-off first-light proof) and a new
`print_imx296_ae_regs()` reads IMX296's own `SHS` (0x308D-0x308F, 24-bit
LSB-first — a genuine direct register read, see the SHS-vs-exposure note
above) and `GAIN` (0x3204-0x3205, 16-bit LSB-first) registers directly
over I2C every printed frame (`REG_DUMP_EVERY = 5`, same throttling
OV5647's own `print_ov5647_ae_regs()` uses to bound the RAM console) —
proof of what the writeback actually landed in the sensor, not just what
the library computed as a target. The 20 s `savebin` hold on the last
frame is unchanged. A new `-DAEN_ISP_N_FRAMES=<n>` CMake option overrides
`N_FRAMES` independent of the AE mode (for an AE-off 60-frame diagnostic
run). New `alp_sdk.examples.aen.isp_capture.imx296_ae` build-only
`testcase.yaml` scenario (same shields as `.imx296`, no
`overlay-no-ae.conf`) covers the AE-on build.

**Bench runs 298-300** (E1M-AEN803 2026W36-0001, evening light) found AE
never converging: sensor `SHS` register readback stuck at `0x45C` (1116
decimal → exposure = `lines_per_frame - SHS` = 1118 - 1116 = **2 LINES**,
its floor, NOT "SHS = 2 lines" — see the SHS-vs-exposure note above),
`GAIN` 0, for all 60 frames, `ae_stable` never set, and the library's own
`meanLum` log line printing only 3 times across 60 frames with its OWN
per-restart frame counter resetting each time — proof AE was restarting
from scratch on every ISP stream restart, not running continuously.
Cause (part 1): `isp_pico.c`'s buffer-starvation auto-stop
(`isp_bottom_half()`, when its own output-buffer queue ran empty —
expected every frame at `N_BUFFERS=2`) called a FULL
`video_stream_stop()`/`_start()` on the CSI controller, cycling the CSI-2
endpoint and sensor through STANDBY on every restart; IMX296's datasheet
(p54) says a normal image is only output from the 9th frame after
STANDBY cancel, so the AE loop never saw two consecutive real frames.

**Fix C**: `isp_bottom_half()`'s starvation path (genuine buffer
starvation only — a new local `bool starved`, set ONLY by the "no more
empty buffers" branch; a real `isp_attach_buffer_to_hw()` hardware
failure used to also route down this path via `signal_status ==
VIDEO_BUF_DONE`, wrongly getting the gentle pause instead of a full stop
— reviewer fix) now calls two new `video_alif.c` functions,
`alif_cam_cpi_pause()`/`alif_cam_cpi_resume()`, that pause/resume ONLY
the CPI capture engine (reusing the register sequence
`alif_cam_work_helper()`'s own memory-capture-path starvation pause
already used for this exact purpose, #2287 Stage A) — leaving the CSI-2
endpoint and sensor streaming across the pause. `isp_stream_start()`'s
own restart checks a new `struct isp_data.controller_cpi_paused` flag to
call `alif_cam_cpi_resume()` instead of a full `video_stream_start()`
(which would otherwise `-EBUSY` against a controller that was never
actually stopped); `isp_stream_stop()` (reviewer fix, blocker: the OLD
`if (!data->is_streaming) return 0;` early guard meant a user stop/close
landing right after a starvation pause — which already sets
`is_streaming = false` without touching the controller — never reached
`video_stream_stop(controller, ...)` at all, leaving the sensor + CSI-2
endpoint running indefinitely after what looked like a clean stop) now
tears the controller down for real whenever EITHER `is_streaming` OR
`controller_cpi_paused` is set, and always calls `isp_vsi_stop()` in that
case too (the ISP library itself was never stopped by the pause either).
A second reviewer fix closes a race in `video_alif.c`:
`alif_cam_work_helper()`'s ISP-consumer branch restarted the CPI
(`hw_cam_start_video_capture()`) unconditionally, with no lock and no
paused check — a STOP-interrupt work item already queued when
`alif_cam_cpi_pause()` ran could still reach this restart moments later
and undo the pause. A new `struct video_cam_data.cpi_paused`, set/cleared
under the SAME `data->lock` `alif_cam_cpi_pause()`/`_resume()` already
take, closes it.

**Bench run 301** (this Fix C, deployed alone) confirms the
STANDBY-cycling removal itself: the IPI's own `"masked after 16"` log
line — previously printed on every restart — now prints exactly ONCE
across the whole run, and frame rate rose roughly 4x (app-side ~0.8 →
~3.4 fps, ISP-side ~6.7 fps). **AE itself was still stuck** at the SAME
`SHS 0x45C` / `GAIN 0` floor from frame 1, `ae_stable` still never set —
proof the STANDBY-cycling was A cause of lost frames, but not (or not
the whole of) why AE never converges.

**Bench run 302** (an AE-INF-log diagnostic build) narrowed it further:
the library's own `meanLum` measurement runs every app frame (so the
EXPM hardware measurement itself is NOT stuck), but its frame counter
resets to `"Frame 1"` on (almost) every app re-queue — and no
`intLine`/`again` writeback decision is EVER logged, i.e. the AE 3A
module never reaches a convergence decision at all, only ever restarting
its own internal accumulation. Cause (part 2): `isp_stream_start()`
ran its FULL restart sequence — `isp_vsi_update_cfg()` and, critically,
`isp_vsi_start()` (`VSI_MPI_ISP_EnableDev`/`EnablePort`/`EnableChn`,
`isp_api_wrapper.c`) — on EVERY app re-queue, even a resume from a
CPI-only pause where the ISP itself was never `isp_vsi_stop()`'d (the
starvation handler never calls it, see Fix C above) — so these Enable*
calls run on an ALREADY-enabled channel every restart. This is the
strongest candidate for what resets the 3A module's accumulated
state (**not proven against the closed `VSI_MPI_ISP` library's source**
— `isp_vsi_update_cfg()`'s own `SetCalib` is separately once-guarded,
patch 0007, so it is ruled OUT as the resetter; the Enable* calls are the
only remaining candidates that run unconditionally on every restart AND
plausibly reinitialize per-channel state).

**Fix D**: `isp_stream_start()` now takes an entirely different,
lightweight path when resuming from `controller_cpi_paused`: instead of
the full sequence (`isp_vsi_update_cfg()`, `isp_apply_aem_wbm()`, the
`ACQ_PROP` pin-mapping write, `isp_vsi_enqueue()`,
`isp_apply_ae_sensor_gate()`, `isp_vsi_start()`, `isp_apply_mrsz()`, the
`wb_dirty`/`ae_dirty`-gated applies), it does exactly what
`isp_bottom_half()` ITSELF already does for every ordinary continuing
frame — the steady-state case that never touches any of the above:
`isp_attach_buffer_to_hw()` (a pure ISP MI register write, no VSI
library call at all) to hand the ISP its next destination buffer, then
`alif_cam_cpi_resume()` to re-arm the CPI. A first start, or any restart
after a REAL `video_stream_stop()`, is unaffected — falls through to the
existing full sequence. **Bench run 303** confirms this closes the
mechanism run 302 pointed at: the library's own `AeRun`/`intLine` log
lines now appear across the whole capture (`Frame 1..61`, `intLine`
climbing 6 → 18 → 55 → 168 → 514 → 1114, `again` climbing to `0x3ecc1`)
instead of resetting every restart, and the sensor's `SHS`/`GAIN`
register readback tracks that writeback exactly — **AE now runs to
completion and the writeback reaches the sensor**. **Image quality is
still NOT verified**: the captured frame stayed pure noise at the AE
loop's own convergence point (see the SHS/GAIN clamp below) — a
SEPARATE, still-open problem this fix does not address.

Review closed two correctness gaps in Fix D's light resume path itself
(both fixed before the run-303 bench pass above). `data->cpi_paused`
(`video_alif.c`) used to be cleared only in `alif_cam_cpi_resume()` — a
pause → user stop → user start cycle never went through that function,
so a stale `cpi_paused = true` survived into the fresh stream and
`alif_cam_work_helper()`'s own paused check then wrongly skipped
re-arming the CPI on that stream's first STOP interrupt (one frame, then
a stall); now cleared in both `alif_cam_stream_stop()` and
`alif_cam_stream_start()` too. Separately, `isp_set_ctrl()`'s
`wb_dirty`/`ae_dirty` contract ("only the next `isp_stream_start()`
applies a pending AWB/AE ctrl change") and `isp_set_fmt()`'s format
changes both had nothing analogous to guard the light path: a **pending
ctrl change made while paused now forces a full stop-and-restart**
instead of being silently dropped by the light path (`isp_stream_start()`
checks `wb_dirty`/`ae_dirty` before taking it, and unwinds a genuine
`controller_cpi_paused` into a real stop first if either is set); and
**`isp_set_fmt()` on the ISP now returns `-EBUSY` while
`controller_cpi_paused`** instead of silently changing a format the
light resume has no way to notice and apply — the caller must stop the
stream first.

**Fix B**: `isp_apply_ae()`'s int_time/again/dgain seed (`int_time_max/2`,
`again_min`, unity `dgain`) used to be set only for MANUAL mode (`if
(!enable)`) — for AUTO mode the library received int_time/again/dgain=0
(struct's own zero-init). Hoisted out of the `if (!enable)` gate so AUTO
starts from the same sane seed instead of zeros. **This is an
INFERENCE, not confirmed against the closed library's source** — run 301
(Fix B + Fix C together) still showed the identical `SHS 0x45C`/`GAIN 0`
parking despite the STANDBY-cycling being gone, and run 302 points at
Fix D's mechanism (the per-restart Enable* calls) as the more likely
explanation for why AE never reaches a decision at all. Whether the seed
hoist itself does anything is genuinely open.

Also silenced the expected `-ENOTSUP` `LOG_WRN`s from
`isp_apply_ae_sensor_gate()`'s `VIDEO_CID_EXPOSURE_AUTO`/`VIDEO_CID_AUTOGAIN`
sensor-ctrl calls (IMX296 registers neither control — its exposure/gain
are set through `VIDEO_CID_EXPOSURE`/`VIDEO_CID_ANALOGUE_GAIN`, which the
driver writes as `SHS`/`GAIN`) down to `LOG_DBG`, keeping `LOG_WRN` for
any other, unexpected error.

Fix B, Fix C and Fix D all compile 0 (new) warnings on the IMX296 AE-on
build and the unchanged OV5647 default, and `isp_ae_conv` + `video_sensors`
(native_sim) both pass — **re-bench pending** on both IMX296 (does AE now
reach a convergence decision under Fix D) and OV5647 (Fix C/D change the
starvation-pause and restart code path every ISP-consumer sensor shares;
OV5647's own 30-frame AE+AWB capture has not been re-run against either).

AWB/CCM calibration for IMX296 remains unaddressed (falls through to
ARX3A0's defaults, as before this unit).

**Bench run 303** (build with `overlay-ae-diag.conf`, Fix D applied)
confirms Fix D closes the "AE never reaches a decision" mechanism: the
library's own `AeRun`/`intLine` log lines now appear (`Frame 1..61`,
`intLine` climbing 6 → 18 → 55 → 168 → 514 → 1114, then `again` climbing
to `0x3ecc1`), and the `SHS`/`GAIN` register readback tracks that
writeback exactly. **New problem**: the captured frame stays pure noise
even once AE settles at `SHS` register 4 (its floor — the datasheet
permits this exposure ceiling) and `GAIN` `0x1E0` (480, the AE envelope's
own ceiling) — the SAME symptom an earlier RAW (non-ISP) capture at
`SHS`=4/`GAIN`=480 showed. Every failing run so far moved `SHS` and
`GAIN` together (AE always converges both at once), so the two are
confounded — it isn't established which one (or a combination, or
neither) is the actual cause.

`examples/aen/aen-isp-capture` gains two independent manual control-
capture CMake options for the IMX296 variant, AE-off only
(`overlay-no-ae.conf`): `-DAEN_ISP_IMX296_EXPOSURE_LINES=<n>`
(`VIDEO_CID_EXPOSURE`, lines, 1..1114) and `-DAEN_ISP_IMX296_GAIN=<n>`
(`VIDEO_CID_ANALOGUE_GAIN`, 0.1 dB tenths, 0..480) — each independently
sets ONE manual value on the sensor directly (not through the AE
library) before `video_stream_start()`, prints the `video_set_ctrl()`
return code, and reads back the resulting `SHS`/`GAIN` registers once
before capture starts, so a specific exposure/gain combination can be
held for a whole run regardless of what AE would have converged to.

Verified `GAINDLY` (0x3212, datasheet pp.41/56 — "other values
prohibited" besides 0x08/0x09): `zephyr/drivers/video/imx296.c` never
references this register at all (no `0x3212` string, no `GAINDLY`
symbol) — the driver relies entirely on the sensor's power-on-reset
default for it, never writing it to any value.

**Bench runs 304-306** (E1M-AEN803 2026W36-0001, AE off, 20 frames, ROI
through the ISP) used the new control-capture options above to
de-confound `SHS` and `GAIN`:

- Run 304 (`SHS`=4/1114 lines, `GAIN`=0): completely flat, black frame,
  no scene content at all — with gain held at 0 throughout, gain is
  RULED OUT as the cause; `SHS`=4 itself breaks imaging on this board,
  even though the datasheet's SHS legal-range table (p.60) lists it as
  valid. **Not unexplained**: the ROI mode's OWN frame-rate table
  (p.53/p.61) tabulates lines-per-frame as `VTR = ROIWV1 + 30 = 990`
  (this driver's `ROIWV1` = 960) — but `imx296.c` deliberately keeps
  `VMAX` at its all-pixel-scan-mode value, 1118, in ROI mode too (see
  that file's own ROI-mode comment). Run 304's `SHS`=4 failure was
  observed in a lines-per-frame configuration the datasheet never
  itself tabulates `SHS`=4 against — the 1104-line clamp (below) is a
  bench-proven WORKAROUND for this untabulated `VMAX`=1118 ROI
  configuration, not an isolated root cause.
- Runs 305/306 (`SHS`=14/1104 lines, `GAIN`=240 or 480): a real scene IS
  visible, but only in one horizontal band (the middle third at 48 dB,
  the top third at 24 dB) — the rest of the frame is horizontally-streaky
  texture that does not match the scene, as if the kept ISP frame
  straddles two different sensor frames, with the vertical phase of the
  split differing per run.
- Readback always matched the set values in every run (the writeback
  path itself is not in question here).

This IMX296 ROI capture is the FIRST workload to stream multiple
continuous frames through the ISP in DW CSI-2 Controller-timing mode
(the derived `csi-hline`/`csi-vtotal` timing,
`raspberry_pi_global_shutter_camera.overlay`, hsd 679/vfp 153,
UNBENCHED status noted when that overlay was added) — the earlier
single-frame first-light proof (Stage B unit 2) never exercised a
frame-to-frame boundary under this timing at all. `CSI_IPI_ADV_FEATURES`
bit 24 (`SYNC_EVENT`, `csi2_dw_ipi_advanced_features()`,
`video_csi_dw.c`) — which re-locks the IPI's frame-boundary tracking to
the CSI-2 protocol's own Frame Start packet on every frame, rather than
the controller inferring frame boundaries purely from the video-packet
stream — is currently cleared (this controller's existing, unrelated-to-
IMX296 default). A frame-boundary mistrack under the ROI's own derived
timing is a plausible (not proven) explanation for the horizontal-band
symptom in runs 305/306.

Fixes and diagnostics from this round:

- **AE envelope ceiling clamped**: `IMX296_AE_MAX_INT_LINE` (hal_alif
  patch 0013's `imx296_ae_envelope.h`, `sensor_attributes.h`'s IMX296 arm)
  and `imx296.c`'s own `VIDEO_CID_EXPOSURE` control range both move from
  `VMAX - 4` (1114, the datasheet's own legal `SHS` floor) to
  `VMAX - IMX296_SHS_DEFAULT` (1104, `SHS`=14 — this driver's own,
  independently bench-proven power-on-reset default) — run 304's finding
  that `SHS`=4 breaks imaging, with the cause still unknown, means AE
  must not be allowed to converge there. `IMX296_SHS_MIN` (4) itself is
  UNCHANGED — it is still the datasheet-legal floor everywhere else this
  driver cites it; only the exposure-control ceiling this bench round
  found unsafe is clamped. `tests/zephyr/video_sensors`'s
  `test_exposure_ctrl_programs_shs_inverted`/
  `test_exposure_above_ceiling_is_rejected` updated to assert against the
  new 1104-line ceiling (`SHS`=14) instead of 1114 (`SHS`=4). Patch 0013
  regenerated against a fresh clean `v2.3.0`+0001-0012 clone (never the
  shared `modules/hal/alif`) and `patches.yml`'s sha256 updated.
- **`GAINDLY` (0x3212) now written**: datasheet p.41 (verbatim): "08h:
  Gain reflect at the frame" / "09h: Gain reflect at the next frame
  (Same timing as SHS reflecting output.)" — every OTHER value is
  "Setting prohibited", including this register's own power-on-reset
  default, `00h`, which the driver had silently left in place the whole
  time gain has ever been written on this board (confirmed unwritten
  last bench round). `imx296_init()` now writes `09h` unconditionally
  (an earlier revision of this entry had the `08h`/`09h` decode
  backwards and used `08h`) — the datasheet's own "(Same timing as SHS
  reflecting output.)" parenthetical says `09h` matches `SHS`'s own
  next-frame latch timing (p.60), so `GAIN` and `SHS` land together on
  the same frame instead of `GAIN` taking effect one frame earlier than
  the exposure it was paired with by
  `isp_api_wrapper.c`'s AE writeback. Same "hardware and control cache
  start in agreement" reasoning as the existing `SHS`/`GAIN`/`REVERSE`
  init writes. **Not independently proven to be THE cause of the
  noise/streak symptoms above** — a real datasheet violation and timing
  correctness fix regardless — isolated as build C4's own variable (see
  below), and bench runs 307/308 found no effect on the symptom (see
  below); the actual cause was unrelated (see the CPI re-arm fix,
  below).
- **New, NOT-yet-enabled DT property** `ipi-fs-sync`
  (`snps,designware-csi.yaml`) sets `CSI_IPI_ADV_FEATURES_SYNC_EVENT`
  (`csi2_dw_ipi_advanced_features()`, `video_csi_dw.c`) when true; default
  false, byte-identical existing behaviour. Deliberately NOT enabled on
  `raspberry_pi_global_shutter_camera.overlay` or any other shield yet —
  isolated as build C5's own variable, a scratch-only DTC overlay, not a
  tracked shield change.

Builds C4 (SHS=14/gain 240, same exposure/gain as the earlier C3 control
capture, now with the `GAINDLY` fix — isolates `GAINDLY` alone) and C5
(C4 + `ipi-fs-sync` enabled via a scratch DTC overlay — isolates IPI
frame-sync alone) are pending the next bench round.

**Bench runs 307/308** (C4/C5, isolating `GAINDLY` and `ipi-fs-sync`):
neither changed the underlying symptom — run 307 (`GAINDLY` build): scene
band at rows 0-255 plus a thin strip at the bottom of the frame; run 308
(`ipi-fs-sync` build): scene band at rows 832-895 — the band MOVED
between runs, it was never at a fixed row range, and neither `GAINDLY`'s
value nor the IPI frame-sync setting changed that. Both remain real
fixes on their own datasheet/robustness merits (kept), but neither is
the cause.

**Cause (advisor code analysis)**: the CPI runs in SNAPSHOT mode
(`ensemble_e8_peripherals.dtsi:1701-1711` declares no capture-mode
override, so `hw_cam_start_video_capture()`'s `CAM_CTRL_SNAPSHOT` bit
applies — one frame captured per `START`, then the hardware auto-stops
itself). `alif_cam_work_helper()`'s ISP-consumer (non-`axi_bus_ep`)
STOP-work branch (`video_alif.c`) used to re-arm the CPI
(`hw_cam_start_video_capture()`) UNCONDITIONALLY and IMMEDIATELY on every
STOP interrupt — racing `isp_pico.c`'s own frame-end interrupt and bottom
half, which is what actually decides (per frame) whether there is a next
destination buffer at all. That re-arm captured a frame the ISP never
asked for; `alif_cam_cpi_pause()`
(`hw_cam_cpi_only_stop()`: `CAM_CTRL=0` + soft reset, no frame-boundary
wait) then cut THAT unwanted, already-running capture mid-frame when the
ISP's bottom half decided to pause — and the ISP's own MI stayed
targeting the buffer it had just handed to the app
(`isp_pico.c`'s `isp_bottom_half()` never retargeted or disabled the MI
before this). The result: an extra frame partially overwrote the kept
buffer (frame_end_count running roughly 2x the app-visible frame count),
and the light resume path never re-synced the ISP core to the actual CPI
state — consistent with runs 305-308's captured frame visibly straddling
two sensor frames.

**Fix**: CPI re-arm responsibility moves to the ISP, the only thing that
actually knows whether a next destination buffer exists.
`alif_cam_work_helper()`'s non-AXI STOP-work branch (`video_alif.c`) no
longer re-arms the CPI at all — it does nothing (the STOP interrupt is
otherwise unused in ISP-consumer mode; no `fifo_in`/`fifo_out`
bookkeeping happens there either). `isp_pico.c`'s `isp_bottom_half()`
now calls the new `alif_cam_cpi_resume()` itself, but ONLY right after a
successful `isp_attach_buffer_to_hw()` for the next frame — attach (ISP
MI target + `CFG_UPD`) always happens before re-arm (CPI `START`). On
starvation, `isp_bottom_half()` simply does not call
`alif_cam_cpi_resume()` — no active register action at all, since
nothing else can re-arm the CPI behind its back any more.
`isp_stream_start()`'s Fix D light-resume path (enqueue after
starvation) already attaches then resumes in that order, so it needed no
change beyond continuing to work. The corrupted-frame ISR path
(`isp_isr_handler()`'s `INTR_SIZE_ERR`/`INTR_DATALOSS`/
`isp_intr_err_mask` branch, which used to skip `cb_work` — and now would
also skip the re-arm — entirely) submits a new, minimal
`cpi_rearm_work` work item instead of stalling the stream after one bad
frame; it exists as a separate work item because `alif_cam_cpi_resume()`
takes a `k_mutex`, unsafe to call directly from ISR context.

**Simplified as a consequence**: `alif_cam_cpi_pause()` and
`struct video_cam_data.cpi_paused` (`video_alif.c`/`.h`) are GONE — with
nothing else ever re-arming the CPI behind `isp_pico.c`'s back, a pause
needs no active register action and no race-guard flag to protect it
(the register-stop `hw_cam_cpi_only_stop()` this used to call was
PRECISELY the mid-frame cut the root-cause analysis above identifies).
`isp_pico.c`'s own `controller_cpi_paused` bookkeeping (the Fix D
light-vs-full start decision, and the was-paused-must-still-tear-down
`isp_stream_stop()` fix) is UNCHANGED — it still needs to know whether
the last pause was a CPI-only one, now simply set directly by
`isp_bottom_half()` on starvation instead of via a call into
`video_alif.c`. A dead `if (data->controller_cpi_paused) { ... } else {
video_stream_start(...) }` branch in `isp_stream_start()`'s own full
sequence (provably unreachable with the true condition, now that the
light-resume path and the dirty-ctrl unwind both already handle every
case where the flag could be true) was also removed.

The OV5647 ISP path is the SAME code (`isp_bottom_half()`/
`alif_cam_work_helper()` do not branch on which sensor is bound) and is
therefore covered by this fix identically, untested independently here.
The non-ISP raw-CPI memory-consumer path (`axi_bus_ep` set,
`alif_cam_work_helper()`'s OTHER branch, Stage A's own starvation
pause/`starved`/`fifo_in` bookkeeping) is UNTOUCHED by any change in
this round.

**Bench runs 309/310** (C6/A8, E1M-AEN803 2026W36-0001, night room)
**confirm this fix**: run 309 (AE off, `SHS`=14/1104 lines, `GAIN`=240,
20 frames) — `frame_end_count` advances by EXACTLY +1 per app frame (the
extra frame from the old re-arm race is gone), a WHOLE-FRAME scene
(per-64-row high-frequency-content ratio 0.95-0.97 across every band of
the frame, vs. runs 306-308's streaked bands at 1.5-1.7), ~3.8 app fps,
one IPI mask window across the whole run. Run 310 (AE on, 60 frames) —
AE converges upward from a genuinely dark scene: `intLine` climbs
6 → 18 → 55 → 168 → 514 → 1104 (its clamped ceiling), `again` climbs to
`0x3ecc1` (48.0 dB, its own ceiling); Y-plane mean 53 → 83; the library's
own `meanLum` 24000 → ~44000; `ae_stable` never reaches 1 — consistent
with a low-light limit (AE parked at BOTH its `intLine` and `again`
ceilings simultaneously, with nowhere left to climb), not confirmed
with a brighter scene; a whole-frame scene throughout; `U`/`V` mean
144/108 (AWB
off, uncalibrated, expected). The CPI re-arm fix (this changelog entry,
above) is therefore **bench-confirmed**, not just code-certain.

**Bench run 312** (camera-mjpeg-stream, IMX296, E1M-AEN803
2026W36-0001, night room, Stage B unit 5): pipeline confirmed end to
end — DHCP lease obtained (192.168.10.102), `/stream` and
`/snapshot.jpg` both served a valid 1280x960 JPEG. AE ran gain to the
GAIN register's full 48.0 dB ceiling in this dim scene; the resulting
frame noise blew `MJPEG_HTTP_MAX_JPEG` (160 KiB) on every subsequent
encode ("E: JPEG buffer full" / `alp_jpeg_encode` rc=-7
`ALP_ERR_NOMEM`, 560x in 40 s, `retry=0` the whole run). Three fixes,
this round:

- **`examples/connectivity/camera-mjpeg-stream/src/main.c`'s quality
  ladder never actually engaged.** Its retry gate keyed off `out_len`,
  which `src/backends/jpeg/alif_hantro.c`'s `video_dequeue()`
  `-ENOSPC` (JPEG_BUFFER_FULL) failure path never set (only the
  separate `done->bytesused > out_cap` path does) -- `out_len` stayed
  0 and `out_len > JPEG_OUT_CAP` was always false. Fixed at both ends:
  `alif_hantro.c`'s `-ENOSPC` path now sets `*out_len = out_cap + 1`
  ("too big, amount unknown"), and `main.c`'s gate
  (`jpeg_quality_should_retry()`, `src/jpeg_quality_ladder.h`) now
  keys off the error code alone, which is simpler and correct for
  BOTH of `ALP_ERR_NOMEM`'s backend-internal causes, not just the one
  that happens to report a size. The chosen quality also now PERSISTS
  across frames (previously reset to `JPEG_QUALITY_DEFAULT` every
  single frame, so a persistently noisy scene would have re-failed
  the same way forever even with the gate fixed) and recovers back
  toward the default one rung at a time after a streak of clean
  encodes (`jpeg_quality_step_up()`).
- **`hal_alif` patch 0013's `IMX296_AE_MAX_AGAIN`** dropped from 257217
  (48.0 dB, the GAIN register's full range) to 16229 (24.0 dB) --
  datasheet p.56 splits the register at 240/0-24.0 dB (analog) vs.
  241-480/24.1-48.0 dB (digital); digital gain multiplies sensor read
  noise along with signal, so capping the AE library's own autonomous
  ceiling at the analog half keeps it out of the noisier digital half
  entirely. `VIDEO_CID_ANALOGUE_GAIN`'s driver-side control range
  (`zephyr/drivers/video/imx296.c`) is UNCHANGED at the full 0-480 --
  only the AE library's own ceiling moved; a caller setting gain
  manually (`examples/aen/aen-isp-capture`'s `AEN_ISP_IMX296_GAIN`)
  can still reach the full register range. Patch regenerated from a
  fresh clean scratch clone at the pinned `v2.3.0` tag with patches
  0001-0012 reapplied first; new sha256
  `540cf3d4d09852e69de881b6f1f679c10d25087e2a0a8af773cd4934db99682c`.
- **The README/Kconfig's missing DHCP lease/URL console lines were
  investigated and are NOT a bug in that print path** (registration
  order, log level, and the callback itself are all correct) -- the
  16 KiB `CONFIG_RAM_CONSOLE_BUFFER_SIZE` circular buffer was wrapped
  many times over by the JPEG-buffer-full failure's own unthrottled
  driver-side `LOG_ERR` (~14x/second for 40 seconds) before anyone
  read it back, evicting the one-time early boot line. Fixed
  indirectly: the two fixes above stop that failure flood from
  happening, so there is nothing left to wrap the line out.

Re-bench after this fix is not yet done -- no run number for the fixed
build yet. Build verification only: `<scratchpad>/build-imx-S2`
(same recipe as S1) -- 0 new warnings, FLASH 262056/262144 B (99.97%,
88 B headroom, unchanged resolution/buffer budget), unchanged OV5647
`camera-mjpeg-stream` default build still 0 new warnings, this
example's own native_sim selftest still passes, and
`tests/unit/mjpeg_quality_ladder` (9/9, including new
`jpeg_quality_should_retry()`/`jpeg_quality_step_up()` coverage) all
pass.

**Bench run 313** (camera-mjpeg-stream, IMX296, E1M-AEN803
2026W36-0001, ba4408aa2's fixes): stream confirmed fixed -- encode
61 fps, 0 fails, 0 retries, ~31 KB JPEGs (no more buffer-full; `again`
converged to `0x3f65` = 16229, the new 24.0 dB cap). One HTTP client
measured 17.6 fps / 552 KB/s delivered at 1280x960, 0.116 s worst-case
frame gap -- send-bound, same shape OV5647's own run 243 found (encode
headroom exists; the send path is the bottleneck). Also found, this
round:

- **`isp_apply_ae()` was pushing the sensor's full 0-480 manual gain
  range as the AE library's ceiling on every `isp_stream_start()`**,
  silently overwriting hal_alif patch 0013's lower 16229 calibration
  ceiling and logging "AE attr mismatch after set" on readback. The
  library's own compiled-in clamp held the real ceiling throughout
  regardless (bench-confirmed: `again` never exceeded 16229) -- this
  was a spurious error log, not a functional regression, but the push
  itself was wrong. Fixed on the alp-sdk side: new Kconfig
  `VIDEO_ISP_VSI_AE_AGAIN_MAX_DB_TENTHS` (default 240, i.e. 24.0 dB,
  when `CONFIG_VIDEO_ISP_VSI_CALIB_IMX296`) clamps the pushed
  `again_max` to agree with the calibration; 0 (every other sensor)
  means unchanged behaviour. No hal_alif change needed for this half.
- **Reverted** `alif_hantro.c`'s bench-run-312 `*out_len = out_cap + 1`
  fabrication on the `-ENOSPC` path -- no longer needed now that
  `main.c`'s retry gate (`jpeg_quality_should_retry()`) keys off the
  error code alone, and `<alp/jpeg.h>` only documents `*out_len` as
  the required size "when known" -- it genuinely isn't known on that
  path, so leaving it untouched is the honest answer.
- **`main.c`'s quality-ladder persistence was too aggressive**: on a
  total encode failure (both the original and the stepped-down retry
  failed), it unconditionally forced `current_quality` down to the
  floor for every LATER frame too -- wrong for a pool-exhaustion
  `ALP_ERR_NOMEM` (`video_import_buffer()` `-ENOBUFS`), which a lower
  quality can never fix. `current_quality` is now only ever lowered in
  the success branch, i.e. only when a lower-quality retry (or the
  current rung itself) actually succeeded; a failure only resets the
  recovery streak, never forces the rung down. `tests/unit/
  mjpeg_quality_ladder`'s pure-function coverage
  (`jpeg_quality_should_retry()`/`jpeg_quality_step_up()`) is
  unaffected -- the removed logic lived in main.c's loop, not in a
  tested pure function.
- **hal_alif patch 0013's header comment corrected**: the analog
  (0-240)/digital (241-480) split is READ OFF p.56's gain-vs-code
  PLOT, not quoted from a labelled table; "digital gain caused the
  overflow" softened to plausible-but-unproven -- run 313's 0-overflow
  result at the new cap is consistent with that explanation but was
  also a different-brightness scene than runs 305/310/312, not a
  controlled comparison. Patch regenerated (same values, wording only)
  from a fresh clean scratch clone; new sha256
  `d9fdb37170ebe23c2725a82fea78d3294ba173f4196a1a49409c03c781de83a2`.
- **DHCP lease/URL lines still never appeared**, even with 0 JPEG
  failures this run -- rules out run 312's flood theory as the WHOLE
  story. The VSI AE/AWB library's own `LOG_INF` chatter
  (`isp_api_wrapper.c`, `CONFIG_VIDEO_LOG_LEVEL`) runs continuously for
  the life of the stream, up to ~13 KB/s -- enough on its own to wrap
  the 16 KiB RAM console ring buffer inside ~2 seconds with no JPEG
  error involved at all. `prj.conf` now sets
  `CONFIG_VIDEO_LOG_LEVEL=2` (WRN) for this app -- a portable, core
  Zephyr per-module log-level symbol, not a vendor-lib edit or a
  change to the global `CONFIG_LOG_DEFAULT_LEVEL`. Re-bench needed to
  confirm the DHCP lines survive with this fix -- not yet done.

Build verification only: `<scratchpad>/build-imx-S3` (same recipe as
S1/S2) -- report FLASH%/warnings/artifacts in the PR/handback, not
hardcoded here (see README's own note on why).
