### Fixed — i2c-master's remaining bare `i2c-scanner` referents, orphaned when #1792 closed #1705 early (#1795)

`examples/peripheral-io/i2c-master/README.md`'s bare cross-reference to
`examples/peripheral-io/i2c-scanner` was fixed in #1792, but that PR carried
`Closes #1705` and auto-closed the issue while a comment on it had already
extended the scope to six more bare instances in non-markdown files, none of
which #1792 touched. This completes that scope.

`scripts/alp_project.py --emit scaffold`'s doc-link rewriter only rewrites
markdown link syntax, so it was never going to reach a C comment or a YAML
comment either way -- there is no "make it a link" fix for `src/main.c`,
`board.yaml`, or `testcase.yaml`. The two explicit run-this instructions in
`src/main.c` ("you may want to run ...", "Use i2c-scanner to enumerate ...")
now spell out that the referent is the alp-sdk repo's own example and is not
part of the customer's scaffolded project. The remaining bare `i2c-scanner`
mention in `src/main.c` is expanded to its full repo-relative path. The
`board.yaml` and `testcase.yaml` mentions, and one softer `src/main.c`
mention, already carried the full repo-relative path and needed no change.

`tests/fixtures/emit-snapshots/scaffold.sensor-v2n101.snap` embeds
`src/main.c` verbatim and is regenerated to match.
