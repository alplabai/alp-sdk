### Added — `validate_metadata.py` now BINDS `memory:` to the population facts it is derived from

Every AEN preset states the derivation in prose —
`metadata/e1m_modules/E1M-AEN801.yaml`: *"dram_mbit  <- 0: on_module.hyperram
is `assembled: false`"* — but nothing bound it. Proven by mutation: setting
`hyperram.assembled: true` while leaving `dram_mbit: 0` was **fully green** —
`validate_metadata.py` rc=0 AND `pytest tests/scripts/` rc=0. `dram_mbit: 128`
against an unpopulated part left only one hardcoded string assertion red. A
derivation nothing binds is not a derivation; it is a comment that happens to
be true today.

`_check_som_memory_population` closes it. `memory.dram_mbit` is decided by
`on_module.hyperram`, `memory.flash_mbit` by every
`on_module.ospi_memories[]` entry:

- every relevant part `assembled: false` ⇒ the figure MUST be `0` — not
  `TBD` (which re-opens a question the preset just closed) and not a
  capacity;
- any relevant part populated ⇒ the figure MUST NOT be `0`, and when every
  fitted part declares an integer `capacity_mbit` it must equal their sum;
- `assembled:` absent reads as **populated**, per the schema's own default
  (*"Population status: true (default)"*) — never as "unknown";
- `assembled: "optional"` is BOM-variant dependent, so the exact capacity is
  not fully decidable — but when an always-fitted part sits alongside it,
  that fitted part still sets a FLOOR: the figure must be `TBD` or ≥ the
  always-fitted parts' summed `capacity_mbit` (an optional sibling can only
  ADD memory, never subtract it). With no always-fitted part in the mix, or
  a fitted part whose own `capacity_mbit` is `TBD`, only the `0`
  contradiction above is decidable — this is the state of
  `E1M-AEN301/401/501/601/701` today (`assembled: optional`,
  `dram_mbit`/`flash_mbit` both `TBD`), and the check binds them (both
  `on_module` blocks present) without forcing a figure;
- a preset declaring neither block is **skipped**: V2N/V2M's LPDDR4X + eMMC
  and E1M-NX9101's open capacities state no population fact here, and
  inventing one to bind against would be inventing a hardware value;
- **except on an `alif:ensemble:*` part, where both blocks are REQUIRED** —
  see below.

**A check you can evade by deleting the input is not a check.** The skip rule
above shipped as an unconditional one, and it was the escape hatch: with both
`on_module` blocks simply absent, `_check_som_memory_population` bound nothing
and printed **no line at all**, so a preset could carry an arbitrary
`dram_mbit`/`flash_mbit` figure with no population fact behind it at rc=0.
Measured on a mutated `E1M-AEN801.yaml` with both blocks deleted and
`256`/`256` restored: `validate_metadata.py` rc=**0**, and the file's only
output was from three *other* checks. The same tree on the fixed script is
rc=**1** with both figures named.

Two changes close it, and both are visible in the gate's own output:

- On an Alif Ensemble part the blocks are mandatory. That die's only external
  memory interface is the OSPI/HexSPI octal bus
  (`metadata/socs/alif/ensemble/e8.json` lists HexSPI + SD/eMMC and no DRAM),
  so `on_module.hyperram` / `on_module.ospi_memories` are not *one* possible
  source for `memory:` — they are its whole source, and omitting one deletes
  the fact rather than leaving a question open. The trigger is the `silicon:`
  ref (`scripts/validate_metadata.py:481` ("refs on which")), never a SKU
  allow-list, so a future `E1M-AEN901` is covered the day it lands —
  `test_the_requirement_is_derived_from_the_silicon_ref_not_a_sku_list` pins
  that.
- An unbound preset prints `SKIP <file> (nothing bound -- declares neither
  on_module.hyperram nor on_module.ospi_memories)`. Silence was
  indistinguishable from a file the loop never reached; the seven
  out-of-scope presets (`E1M-NX9101`, `E1M-V2M101/102/103`,
  `E1M-V2N101/102/103`) now say so on their own line.

JSON Schema cannot reach across `on_module` into `memory:` (nor sum a sibling
object's values), so this layer is the only one that can hold it.
`tests/scripts/test_validate_metadata_som_memory_population.py` pins the same
rules independently, so deleting the check reddens something other than the
check's own file, and includes per-SKU real-preset coverage across all seven
AEN presets: `E1M-AEN801` is the one preset that resolves to `0`/`0`;
`E1M-AEN803` has both memories fitted (`512`/`256`; its `ospi1` is a
placeholder `assembled: optional`, `capacity_mbit: TBD`, so `flash_mbit`
resolves off `ospi0` alone); `E1M-AEN301/401/501/601/701` stay
open (`assembled: optional`, `TBD`/`TBD`) and are asserted bound-but-open, not
forced to `0`.

### Changed — the portability matrix distinguishes "resolved: none" from an unstated capacity, on BOTH figures

`scripts/gen_portability_matrix.py`'s `notes_for()` only tagged a positive
`dram_mbit`, so `E1M-AEN801`'s resolved `0` rendered as silence —
indistinguishable from `E1M-NX9101`'s genuinely-unknown `TBD`. `0` now
renders `no external DRAM`; `TBD` still renders nothing, because a Notes
cell has nothing true to say about it.

The same gap existed one column over: `notes_for()` never read `flash_mbit`,
so `E1M-AEN801` (`0`, resolved: MRAM-only) and `E1M-NX9101` (`TBD`, open)
rendered **identically** — on the figure that decides storage and MCUboot
partitioning. `flash_mbit: 0` now renders `no external flash` alongside it,
and `docs/portability-matrix.md` is regenerated: `E1M-AEN801`'s row gains
`no external DRAM · no external flash`; `E1M-AEN803`'s row keeps its
existing `512 Mbit DRAM` tag (a *positive* `flash_mbit` stays untagged —
printing eMMC/NOR densities is an editorial addition to the cell, not a
distinction anything was collapsing); `E1M-NX9101` gains nothing.

**No behaviour change.** Nothing reads `memory.dram_mbit` or
`memory.flash_mbit` except that one Notes cell, and nothing reads
`ospi_memories[].assembled` or `hyperram.assembled` outside the new check —
the `assembled == "optional"` skip at `scripts/alp_orchestrate/slugs.py:134`
(`dev.get("assembled") == "optional"`) applies only to `i2c_devices[]`. The
one `memory` key a consumer *does* read is a DIFFERENT block: per-core
`cores.<id>.memory` (`heap_kib` / `stack_kib` / `isr_stack_kib`), copied onto
the `Slice` at `scripts/alp_orchestrate/loader.py:584`
(`memory=dict(entry.get("memory") or {}),`) and read back at
`scripts/alp_orchestrate/validate.py:272`
(`heap_kib = (slice_.memory or {}).get("heap_kib")`) and
`scripts/alp_orchestrate/kconfig.py:1511`
(`mem = slice_.memory or {}`). That block never carries `dram_mbit` or
`flash_mbit`, so it is not evidence for the no-consumer claim and must not
be cited as though the SoM-preset `memory:` block flowed through it.

### Known hazard — `partition.py` will still size a partition against an unfitted part

`scripts/alp_orchestrate/partition.py::_resolve_flash_device` keys off
`on_module.ospi_memories[].capacity_mbit` and ignores `assembled:`. Measured:
`_resolve_flash_device("ospi0", <E1M-AEN801 preset>)` returns
`size_bytes=33554432` — 32 MiB, from `capacity_mbit: 256` — on a part
`E1M-AEN801`'s own preset declares `assembled: false`, and
`_known_flash_devices()` still offers `ospi0` and `ospi1` to a customer's
`storage[].flash_device:`. `assembled: false` is the declared fact a fix
keys on; `capacity_mbit` is the DESIGNED-IN part's capacity, not one the
module carries. This is `E1M-AEN801`-specific: `E1M-AEN803` genuinely
carries both parts, and `E1M-AEN301/401/501/601/701` are open
(`assembled: optional`), not `false`, so the same call on those presets is
not this same defect.

Latent, not live: no in-tree `board.yaml` names either device — the only
`flash_device:` user is
`examples/connectivity/production-deployment/board.yaml`, five entries, all
`mram_main`.

Deliberately **not** fixed here, because the fix crosses repos.
`python/tan/planner/partition.py::_resolve_flash_device` in tan-cli is a
line-for-line port of this function, and tan is the executor — a refusal
added on one side only would make alp-sdk and tan disagree about the same
`board.yaml`, which is the drift ADR-0020 exists to prevent. Both sides move
in one lockstep change (refuse on `assembled: false`, and drop the device
from `_known_flash_devices()`), or neither does. The hazard is now recorded
at the call site, so the next reader meets it before the code.
