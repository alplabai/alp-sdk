### Fixed — VERSIONS.md's v0.15.0 row no longer claims the tag was never cut (#1501)

`VERSIONS.md:46` still said `v0.15.0` was "tagged as release candidate only
(`v0.15.0-rc1`, 2026-08-01); no final `v0.15.0` tag exists" and cited
`metadata/sdk_version.yaml` as declaring `version: 0.15.0`. `v0.15.0` shipped
2026-08-07 (an ancestor of `v0.16.0-rc1`/`v0.16.0`), and `sdk_version.yaml` has
long since moved past it. The status cell now reads **released 2026-08-07**;
the two stale sentences are dropped. The row's #1025 note also turned out
stale against the final tag -- it said the status half was "NOT in this
release" because it postdated `v0.15.0-rc1`, but `1a91a232` is an ancestor
of the `v0.15.0` GA tag, so the status half did ship in v0.15.0; the note
now says so.
`docs/abi/README.md`'s matching v0.15.0 note and its missing `v0.16-snapshot.json`
row -- the issue's other two claims -- were already corrected by #1622 before
this fix landed; re-verified against `origin/dev` and left untouched.
