### Documented — the two 2026-08-05 superpowers plans carried no STATUS banner, and a shipped recipe cited a plan doc as unreachable (#1523)

`docs/superpowers/plans/` holds 52 plans on `origin/dev`. Before this change,
42 opened with a `STATUS`/`SUPERSEDED`/`Historical`/`Active` banner marking
whether the plan's work has landed, and 10 had none. Two of the ten, both
dated 2026-08-05 (`2026-08-05-onnxruntime-own-recipe.md`,
`2026-08-05-yocto-cpu-inference-and-ai-library-gaps.md`), open straight into
an autonomous-implementer directive with every task still an unchecked
`- [ ]` — even though most of that work is already in the tree (`onnx` in
`scripts/alp_model/manifest.py`'s `VALID_BLOB_FORMATS`,
`metadata/libraries/onnxruntime.yaml` at 1.28.0,
`src/yocto/inference_ort.cpp` + `src/yocto/CMakeLists.txt` wiring the ORT CPU
backend, `meta-alp-sdk/recipes-devtools/onnxruntime/onnxruntime_1.28.0.bb`
at the `v1.28.0` pin). Both now carry a `> **STATUS**` banner naming what
shipped, kept for implementation-history context — the yocto plan's banner
also flags its own Task 1 ("pin `madgwick-ahrs` to a real SHA") as *not*
landed as written: `metadata/libraries/madgwick-ahrs.yaml`'s `version` and
`west.yml:476`'s `revision` both hold the mutable tag `v1.3.2`, with the
upstream SHA (`015d68494274b479b5996bff2530ecbcfdc266f2`) recorded only in a
comment on each, not as the pin itself. The other eight banner-less plans
(`2026-07-24-alp-model-{check,envelope,measure,prep,zoo}.md`,
`2026-07-24-tan-model-envelope.md`, `2026-07-24-vscode-models-panel.md`,
`2026-07-25-alp-model-tflite-convert.md`) predate #1523's onnx/AHRS scope and
are unchanged by this fix — the plans dir now holds 44 bannered / 8
unbannered.

Separately, `onnxruntime_1.28.0.bb`'s only in-tree justification for
building upstream ONNX Runtime instead of inheriting `nxp-imx` (1.24.3) or
`meta-renesas-ai` (1.8.0) told the reader its rationale doc "does not exist
on this branch" and cited it by PR number (#1249) instead of path. The doc
(`docs/superpowers/plans/2026-08-05-onnxruntime-own-recipe.md`) merged four
days before the recipe did and ships in the same tree; the caveat was already
false at merge time. The parenthetical is deleted and the citation now
points at the doc's path.
