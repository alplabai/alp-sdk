### Documented — drifted line citations refreshed and small stale notes fixed (#2246)

The whole-repo doc audit found a class of drift the automated gates cannot
catch: hand-written `path:line` citations that no longer match the code they
point at. Every re-pointed citation here was re-verified against the current
tree first.

`docs/board-config-emit.md`'s cmake-args section-marker citation now reads
`scripts/alp_project.py:512-515`; `docs/board-config-schema.md` and
`docs/adr/0034` re-point `_enforce_os_matches_core_class` at
`scripts/alp_orchestrate/validate.py:296`, its call site at `loader.py:927`,
and the "A Cortex-A can't run Zephyr" comment at `topology.py:112`.
`docs/tutorials/02-i2c-scan.md` cites the generated routes header
(`alp_e1m_x_evk_routes.h`) for the `XEVK_I2C_ADDR_*` macros instead of the
facade that defines none, re-points the TAS2563 rows and anomaly note, and
fixes its expected-ACK tables: `0x48` on the E1M-EVK is the TAS2563
GLOBAL/broadcast address that always ACKs where an amp is fitted (#1976),
with U32's PRE-RESPIN 0x48 occupation and `0x4B` re-strap recorded on the
right rows and the device count reconciled. `docs/tutorials/05` labels its
opcode table a common subset and points at `gd32-bridge-protocol.md` for the
newer opcodes; `docs/tutorials/07` re-points the `bench-env.sh` GD32_DPIDR
hedge to `:391-396`; `docs/tutorials/13` re-points V2M101's `e1m_i2c0:` block
to `:67-69`; `docs/tutorials/16` names the real `tests/bench/baselines/`
contents; `docs/diagnostics/ALP-B099.md` re-points `supported_boards:` to
`board.schema.json:466`; `docs/aen-bench-bringup.md`'s lead notes the bench
farm moved to E1M-AEN803 (#2226); and `audio-wake-word`'s "TODO(v0.6)"
milestone framing is re-phrased at ten minor versions past that marker.
