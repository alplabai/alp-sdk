### Fixed — `docs/gd32-bridge-protocol.md`'s §8 version history stopped at v0.8 and §3.x still claimed the FFT/FAC dispatch had not landed (#1513)

`PROTOCOL_VERSION_MINOR` has been `9u` since `fce829c8` (#496), but §8's version-history list
still ended at `**v0.8** adds SE_RESET`. Added a `**v0.9**` entry naming the #496 FAC/FFT runtime
dispatch inside `bridge_hw_adc_stream_read()` and the new `CMD_ADC_SPECTRUM_READ` (`0x3A`) opcode
— not attributed to `CMD_ADC_DSP_CHAIN_OPEN`/`STAGE_PUSH`/`CHAIN_BIND` (`0x37`-`0x39`), which are
v0.5-era.

Separately, §3.x's closing sentence told readers to "pick up the bridge-offloaded runtime path
once the FFT/FAC dispatch lands" thirteen lines after the same section says in bold that the
runtime side **is** implemented (#496). The dispatch has been live in
`gd32-bridge-firmware:hal/gd32/adc_stream.c` since that commit; reworded the sentence to
say so and keep `<alp/dsp.h>` documented as the in-RAM host-side alternative.
