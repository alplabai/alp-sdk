### Documentation — `docs/cli.md` and `docs/getting-started.md` overstated `tan doctor --fix`'s sudo safety boundary (#1524)

Both docs said `--fix` was scoped to "elevation-free" manifest commands and
that it "never runs `sudo`" / "only prints commands that need sudo". That
stopped being true as of tan-cli `0eae9c6` (#650, #669): when the caller's
`geteuid() == 0`, `--fix` strips the manifest command's literal `sudo `
prefix and executes the remaining privileged command directly — it never
spawns the `sudo` *program*, but the safety boundary a reader took from the
prose (no privileged system mutation) is gone. In the common shape of an
interactive root shell inside a container, `tan doctor --fix` installs
system packages, exactly what both docs promised it would not do.

Neither doc mentioned the companion TTY gate: `--fix` requires a TTY on
both stdin and stderr (`python/tan/core/consent.py`'s `can_prompt`),
independent of `--ci`/`--non-interactive`/JSON mode. It is not silent when
that gate trips -- `doctor_cmd.py` appends a `doctor.fix-suppressed`
warning naming the condition, `no interactive terminal (stdin/stderr not a
tty -- piped, redirected, or CI)`. The silent no-op is what tan-cli#91 P1
removed.

`docs/cli.md` and `docs/getting-started.md` now both state: a remedy that
needs no elevation (macOS `brew`, Windows `winget`) runs for any caller; a
`sudo`-prefixed remedy (Linux `apt`/`dnf`) is refused with
`doctor.fix-needs-sudo` and gets the command printed to run by hand for a
non-root caller, and has the `sudo ` prefix stripped and the remaining
command run directly for a caller who is already root; and without a TTY on
both stdin and stderr — or under `--ci`/`--non-interactive`/JSON mode —
repairs are disabled and commands are left as printed guidance.

Repo-wide grep for `elevation-free` and the two retired phrasings now
returns zero hits, including the checked-in `doxygen-out/` HTML snapshot:
`doxygen-out/html/md_docs_2cli.html` and
`doxygen-out/html/md_docs_2getting-started.html` carried the same two
retired sentences and are hand-corrected here to match the source docs.
That tree is committed output that only a human ever refreshes —
`pr-doxygen.yml` builds it into the runner's `doxygen-out/` and uploads
`doxygen-out/html/` as a 14-day `actions/upload-artifact`; no workflow
under `.github/workflows/` runs `git commit`/`git push`, and its `paths:`
filter (`include/**`, `docs/doxygen/**`, `docs/adr/**`,
`.github/workflows/pr-doxygen.yml`) would not even have triggered on a
`docs/cli.md`-only change. Nothing in CI would ever have corrected these
two sentences, so a prior draft's claim that regeneration would fix them
was itself false.

Measured lag at this branch's merge-base with `dev`: `docs/cli.md` and
`doxygen-out/html/md_docs_2cli.html` were both last touched by the same
commit (`85879df8`, #1406) — lag zero. `docs/getting-started.md` had two
commits land after that snapshot (`c8f7301c`, #1466; `8afe6232`, #1547).
`docs/getting-started.md` also now scopes its "read-only … safe to run
anytime" claim to "unless you pass `--fix`", so it can't be read as
promising the same no-mutation boundary this issue retracts.

**Squash-message correction.** Commit `46c1539a`'s body still asserts two
things this branch exists to retract: "which makes --fix a silent no-op
without one", and "Repo-wide grep for `elevation-free` and the two retired
phrasings found no other occurrences to fix." Neither is true — the no-TTY
case is not silent (see above), and the doxygen-out grep did find
occurrences (see above). A default squash concatenates every commit body,
so when this PR is squash-merged the merge commit message must carry only
`76d476bf`'s corrected wording for these two points, not `46c1539a`'s.

`tan doctor --help` still prints the retired claim in its `--fix` option
text — "…when it needs no elevation. A command that needs `sudo` is
printed, never run -- tan never spawns sudo." (`python/tan/commands/
doctor_cmd.py:3752` on tan-cli `origin/dev`). That's a tan-cli file, not
alp-sdk; filed as
[alplabai/tan-cli#856](https://github.com/alplabai/tan-cli/issues/856).
