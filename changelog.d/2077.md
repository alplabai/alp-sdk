### Fixed — `tas2563_init()`: real bus status, mandatory reset sequence, SD_N glitch, and shared-net EVK facts (#2077)

`include/alp/chips/tas2563.h` now carries `@par Driver status: [partial-impl]`
(`:10-16`, matching `metadata/chips/tas2563.yaml`'s `driver_status: partial`)
and a `@par Why an in-tree driver, not the upstream one` note (`:18-27`):
an upstream Zephyr TAS2563 driver exists
(`zephyrproject-rtos/zephyr#103148`, merge commit `95bcb74aa`, merged
2026-05-01). It does NOT postdate this repo's pinned Zephyr revision
(`west.yml:221` pins `v4.4.1`, tagged 2026-06-10, which is AFTER the merge
date) -- the accurate fact, checked directly against the local Zephyr
checkout, is that `95bcb74aa` is not an ancestor of `v4.4.1`
(`git merge-base --is-ancestor 95bcb74aa v4.4.1` fails; the commit object
itself resolves fine, so this is a real branch-divergence check, not a
missing-object false negative), so it is main-only and was never
backported to the v4.4 release branch -- confirmed by the absence of any
`tas2563` file anywhere in a local `v4.4.1`-tagged Zephyr checkout. By
maintainer decision this repo keeps its own driver for now.
Neither line existed before this issue, on top of everything below.

`tas2563_init()`'s I2C connectivity probe remapped every bus failure to a
hardcoded `ALP_ERR_NOT_READY`, discarding the status `alp_i2c_write()`/
`alp_i2c_write_read()` actually returned (`chips/tas2563/tas2563.c:251-284`).
A NACK is documented to return `ALP_ERR_IO` (`include/alp/peripheral.h:436-437`).
All three probe/park bus calls now `return s;`, propagating the real status;
`include/alp/chips/tas2563.h`'s `@return` block documents the `(other)` arm.
Neither real caller (`examples/aen/aen-evk-demo/src/main.c`,
`examples/peripheral-io/i2c-device-hub/src/main.c`) branched on
`ALP_ERR_NOT_READY` specifically, so neither needed a behavioural change.

**Reset sequence.** SLAA954 "TAS2563 End System Integration Guide" §3.1 Case
1 recommends both a hardware and a software reset before init; SLASET3D
§9.2 requires a 100 us settle after either, before any I2C access. This
branch adds a new PUBLIC macro for that wait, `TAS2563_RESET_SETTLE_US`
= `200u` (`include/alp/chips/tas2563.h:261`), one constant covering both
the hardware- and software-reset cases -- it did not exist before this
branch. `docs/abi/v0.16-snapshot.json` is regenerated to capture it as an
`ADDED` entry; per `docs/contribution.md`'s ABI policy ("Pre-1.0: API may
change between minors during development") and `docs/abi/README.md`
("Pre-1.0 the diff is informational — additive changes are allowed
between minor releases" and the freeze gate "blocks only on a `REMOVED`
public symbol"), an additive macro needs nothing beyond the regenerated
snapshot: no version bump, no deprecation window, no separate sign-off.
`tas2563_init()` now issues a software reset
(`SW_RESET`, SLASET3D §7.5.3) after book 0/page 0 selection and before any
other configuration (`chips/tas2563/tas2563.c:251-269`) — the part had never
written this register before. This runs unconditionally, whether or not the
function owns `sd_n`; when it does own `sd_n`, driving SD_N high only
*releases* hardware shutdown (it is not itself a hardware reset unless SDZ
was actually low beforehand) — an explicit hardware reset is the caller's
job, not this function's, since it may not own the pin exclusively (see
below). The pre-existing "park in software shutdown" write after the probe
is now redundant given the unconditional software reset, and says so in its
comment (`chips/tas2563/tas2563.c:276-284`).

**SD_N glitch, found by reading `drivers/gpio/gpio_dw.c` (the AEN801 `gpio5`
backend, `snps,designware-gpio`).** `dw_pin_config()` switches a pin's
direction to output BEFORE touching its data register, and only touches
that register for `GPIO_OUTPUT_INIT_HIGH`/`_LOW` — flags `alp_gpio_configure()`
never passes. Configuring SD_N as output before writing it therefore exposed
the data register's reset-time level as an output the instant direction
flipped — an unplanned pulse on a net shared with U28 (see below). That
reset-time level is NOT verified as `0` on this board's actual AE822 silicon
— the DesignWare IP's data-register reset value is a synthesis-time
parameter, so it is possible for it to reset `1` instead; the fix does not
depend on which. Fixed by writing high BEFORE configuring
(`chips/tas2563/tas2563.c:224-230`): `alp_gpio_write()` has no dependency on
the pin already being configured, and `gpio_dw_port_set_bits_raw()` writes
the register unconditionally regardless of direction — but only if `sd_n` is
declared `GPIO_ACTIVE_HIGH` in its devicetree pin-array entry, since Zephyr
only applies a `GPIO_ACTIVE_LOW` entry's inversion inside
`gpio_pin_configure()` itself, not before; documented as a precondition
(`include/alp/chips/tas2563.h:159-174`). Not a live bug today: no in-tree
board declares `P5_2` in `alp,pin-array` at all, and both real callers pass
`sd_n = NULL`. That first write alone is not sufficient on every backend:
Zephyr's `gpio_emul` (this test suite) masks a write against the pins
*currently* configured as output, so a write issued before configure is
silently dropped there if the pin was not already an output — order-dependent
across test cases, and confirmed by mutation (below). `tas2563_init()`
therefore writes high a SECOND time, after configuring, so the final state
lands correctly on both kinds of backend. Verified against Zephyr/DesignWare
and `gpio_emul` only; the `sw_fallback`/`testing` GPIO backends have no real
pin either way, and the CC3501E GPIO proxy's behaviour on a pre-configure
write is unverified — all documented in `include/alp/chips/tas2563.h:116-157`.

**Shared SD_N/IRQZ — an E1M-EVK hardware fact, not a driver defect.** The
authoritative carrier netlist shows `AMP.ENABLE` (SD_N) and `AMP.FAULT`
(IRQZ) are each ONE net shared between U27 and U28 (unlike TI's stereo
reference design, SLAA920 Figure 22, which keeps each amp's shutdown
independent); SD_N carries a 10 kOhm pull-up (R138) to `+VIO`, IRQZ a 10 kOhm
pull-up (R124). This driver models one physical chip per `tas2563_t` and
keeps no cross-instance registry, so the fix is a documented caller contract
(`include/alp/chips/tas2563.h:91-114`): pass `sd_n` to at most one instance
per net, initialise that instance first, and disambiguate a shared fault pin
by reading each instance's own faults. `metadata/boards/e1m-evk.yaml`'s
`EVK_I2C_ADDR_TAS2563_LOW` (line 300-301), `EVK_PIN_AMP_FAULT` (line 344) and
`EVK_PIN_AMP_ENABLE` (line 346) entries record the facts once; regenerated
into `include/alp/boards/alp_e1m_evk_routes.h` via `scripts/gen_board_header.py`.

**Demo (`examples/aen/aen-evk-demo/src/main.c`).** The sound phase's
microsecond-scale `GPIO_OUTPUT_INACTIVE`-then-`gpio_pin_set(1)` pulse was not
GUARANTEED to reach Hardware Shutdown: with SDZ_MODE at its default 00b
("Normal Shutdown with Timer", SLASET3D §7.3.11.1 Table 7-6), asserting SDZ
low starts a graceful ramp-down/power-down sequence that reaches Hardware
Shutdown once it completes; `SDZ_TIMEOUT` (Table 7-7:
2/4/6(default)/23.8 ms — whichever this part's register currently holds,
unknown to the demo) is only a backstop that forces it if that sequence
hasn't finished in time, not the sole path there — and that guarantee itself
assumes `SDZ_MODE`'s default; `SDZ_MODE = 10b` ("Normal Shutdown", no timer
at all) has no time bound whatsoever. SD_N is now held low for
`AMP_ENABLE_RESET_HOLD_MS` (24 ms,
`examples/aen/aen-evk-demo/src/main.c:4114` ("#define AMP_ENABLE_RESET_HOLD_MS 24u")), at or above the
worst-case 23.8 ms, before release — an actual hardware reset, unverified on
real silicon. `TAS2563_RESET_SETTLE_US` is still waited afterward, separately
(`main.c` phase 11).

**The demo's tone could not have reached either speaker, per the datasheet
and driver source -- nothing in this entry has been run audibly on
silicon.** Phase 11 configured both amps with `TAS2563_RX_SLOT_FROM_ADDR`
(the pre-fix call site, since superseded by the explicit
`TAS2563_RX_LEFT`/`_RIGHT` mapping below) on a 2-channel-wide I2S frame.
SLASET3D §7.4.2 (p.42): the
default source is "mono from the time slot equal to the I2C base address
offset"; Table 7-3 (p.30): the offset is `address - 0x4C`, so U27 (0x4D) is
offset 1 and U28 (0x4E) is offset 2. A 2-slot frame only has slots 0
(left) and 1 (right): U27 landed on the RIGHT slot, and U28's slot 2 fell
"either partially or fully beyond the frame boundary," which SLASET3D
§7.4.2 (p.42) again states returns "a null sample equivalent to a digitally
muted sample" -- by this analysis U28 would have been permanently muted by
construction. U27 fares no better on paper: the demo's I2S config was mono
(`.channels = 1`), and the Alif DW I2S driver's mono TX path
(`zephyr/drivers/i2s/i2s_dw.c`, vendored in this tree) writes the sample to
the LEFT slot and a hardcoded `0` to the RIGHT slot -- so the RIGHT slot
U27 was listening to would have carried nothing but zeros regardless of
what `TAS2563_RX_*` it was configured for, on paper, for every earlier
phase (mux routing, `tas2563_init()`, `ACTIVE` mode) that reported success.
No audio was ever run against real amps or speakers to confirm this --
see the SAFETY note below for why it still cannot be, on this board
revision.

Fixed with genuine stereo, not downmix: `amp_i2s_cfg`/`alp_audio_out_open()`
now request `.channels = 2`
(`examples/aen/aen-evk-demo/src/main.c:4336-4340` ("const alp_i2s_config_t amp_i2s_cfg"),
`examples/aen/aen-evk-demo/src/main.c:4442-4445` ("spk = alp_audio_out_open")), and the tone buffer
(`examples/aen/aen-evk-demo/src/main.c:4499` ("tone_buf[SOUND_FRAMES_PER_BLOCK * 2u]"))
is interleaved `[L0,R0,L1,R1,...]` with the identical sample
written to both channels -- the DW driver's stereo TX path writes both
slots for real once the stream is genuinely 2-channel, so downmix was not
needed. Each amp is now configured with its own explicit channel via a new
`amp_rx_channel[AMP_COUNT]` array parallel to `amp_addrs[]`
(`examples/aen/aen-evk-demo/src/main.c:4130` ("Parallel to amp_addrs")):
`TAS2563_RX_LEFT` for U27, `TAS2563_RX_RIGHT` for U28, per the
authoritative 2626-R2 netlist -- U27 drives J14 (`LEFT_P`/`LEFT_N`), U28
drives J21 (`RIGHT_P`/`RIGHT_N`), now recorded in
`metadata/boards/e1m-evk.yaml`'s `EVK_I2C_ADDR_TAS2563_LOW`/`_HIGH` entries
(regenerated into `include/alp/boards/alp_e1m_evk_routes.h`) rather than
hardcoded with no citable source. `SOUND_VOL_START`/`SOUND_VOL_CEILING`
and the `±20000` amplitude are unchanged.
`examples/aen/aen-evk-demo/src/sound_verdict.h`'s PDM-energy ratio/floor
(`SOUND_ENERGY_RATIO_NUM`/`_DEN`, `SOUND_ENERGY_FLOOR`) needed no change:
the check is agnostic to how many amps or channels are actually driven, so
fixing silence into real output can only raise `during_energy`, never
lower it.

`tas2563_rx_channel_t`'s doc (`include/alp/chips/tas2563.h:293-321`) and
the demo's phase-11 step-5 comment
(`examples/aen/aen-evk-demo/src/main.c:4001` ("tas2563_configure_i2s() on every initialised amp"))
both now carry the frame-boundary math and cite
SLASET3D §7.4.2/Table 7-3 (p.30, not p.29 as an earlier draft of this note
said) directly, rather than the old (wrong for a 2-slot frame) "SLOT_FROM_ADDR
on both ... does not need to hard-code which is L/R" guidance.
`test_tas2563_configure_i2s_writes_tdm_fields`
(`tests/zephyr/chips/src/test_audio.c:497`) already asserted `TDM_CFG2`
bits `[5:4]` = `01b`/`10b` for `TAS2563_RX_LEFT`/`_RIGHT` against the fake
before this fix, so no new test was needed for the register-write side of
this change -- the bug was in which channel value the demo passed and how
many host channels it opened, not in what the driver did with a given
channel value.

**MAJOR, follow-up: `RX_SLEN` was still wrong even with the LEFT/RIGHT fix
above.** The Alif DW I2S3 peripheral (`zephyr/drivers/i2s/i2s_dw.c`)
hardcodes `.cfg.wss_len = WSS_LEN` (32) and derives `sclk = 2 * 32 *
sample_rate`, so SBCLK/FSYNC is always 64 -- a 2-slot frame of 32 bits
each -- independent of `alp_i2s_config_t.word_bits`. SLASET3D §7.4.2
(p.39) states outright: "The device supports 2 time slots at 32 bits in
width and 4 or 8 time slots at 16, 24 or 32 bits in width." A 2-slot frame
has exactly one legal slot width, 32 bits. `word_len_codes()`
(`chips/tas2563/tas2563.c:442-474`) previously mapped word width directly
to slot width (word-equals-slot), so a 16-bit `word_bits` (the demo's
`amp_i2s_cfg.word_bits = 16`) produced `RX_SLEN = 00b` (16-bit slot), which
divides the actual 64-bit frame into 4 slots of 16 bits, not 2 of 32. Under
that mapping U27 (`RX_LEFT`, slot 0) read real left-channel data by luck --
slot 0 of a 4x16 division happens to line up with the low 16 bits of the
frame's first 32-bit half -- but U28 (`RX_RIGHT`, slot 1) read PADDING: the
second 16-bit quarter of the left channel's own 32-bit slot, not the right
channel's word at all. So the old config was silent for a DIFFERENT reason
than the LEFT/RIGHT-only narrative above claimed: even after fixing which
`TAS2563_RX_*` each amp used, U27 sat on slot 1 of the true 64-bit frame
(padding, still within the left word) and U28 sat on slot 2 (the
always-zero right word of what a mono-shaped 16-bit-slot view of the frame
saw as a 4-slot layout) -- neither amp was reading the actual right-channel
32-bit slot. Fixed by scoping the slot-width rule to the bus this driver
actually opens: `word_len_codes()` now takes `channels` and forces
`RX_SLEN = 10b` (32-bit) whenever `channels == 2`, regardless of word
width; every other channel count keeps the old word-equals-slot mapping,
since SLASET3D allows 16/24/32-bit slots on a 4- or 8-slot TDM frame and no
host bus in this driver has ever opened more than 2 channels
(`chips/tas2563/tas2563.c:405-474`, doc comment above the function).
`amp_i2s_cfg.channels = 2` (`examples/aen/aen-evk-demo/src/main.c:4336-4340` ("const alp_i2s_config_t amp_i2s_cfg"))
already set the field this rule keys on; no demo-side change was needed
beyond the driver fix and the comment updates at
`examples/aen/aen-evk-demo/src/main.c:4127-4141` ("Parallel to amp_addrs")
and `include/alp/chips/tas2563.h:293-321`. The Alif I2S3 bit-clock
divider this depends on (`zephyr/drivers/i2s/i2s_dw.c:113-121`) is itself
BENCH-UNVERIFIED -- the SBCLK/FSYNC ratio actually achieved on real silicon
has not been measured; this fix corrects the register programming for the
ratio the driver source says it configures, nothing has been heard.
`test_tas2563_configure_i2s_writes_tdm_fields`
(`tests/zephyr/chips/src/test_audio.c:497-590`) is rewritten: every case
uses `ALP_I2S_CONFIG_DEFAULT`'s `channels = 2`, so `RX_SLEN` is now `10b`
in every one of its four cases regardless of word width (previously `00b`
for the 16-bit cases, which asserted the very bug this fix removes -- the
review that caught this called it out explicitly: "currently asserts
`RX_SLEN=00b` for 16-bit and locks the bug in"). A fifth case
(`channels = 1`) was added to prove the `channels == 2` rule does NOT
fire for a mono bus, so the rule stays scoped rather than becoming a
blanket "always 32-bit" change nothing here can observe going wrong.
Mutation-checked: reverting `word_len_codes()`'s `channels == 2` branch to
`(void)channels;` (unconditional word-equals-slot) turns exactly
`test_tas2563_configure_i2s_writes_tdm_fields` red (282/283), then restored
and the suite re-confirmed green (283/283).

**SAFETY, MAJOR: EVK rev 2626-R2's U46 is wired backwards for playback --
this phase no longer enables it or starts I2S playback on this board by
default.** Confirmed against the authoritative netlist
(`E1M-EVK-2626-R2_pinmap.csv`, queried via `grep -a ',U46,'`): U46's
`I2S0_WS`/`I2S0_SCLK`/`I2S0_SDO` nets are `1Y`/`2Y`/`3Y` -- the mux's
OUTPUTS -- not its `1|0`/`1|1`/etc. inputs. The 74LVC157 is a unidirectional
2:1 mux: signal can only flow from its `nA`/`nB` inputs to its `nY`
outputs, never the reverse. Since `I2S0_WS`/`SCLK`/`SDO` are the SoC's own
pads wired to the mux's OUTPUT side, the SoC cannot drive audio out through
U46 to either the TAS2563 amps or the M.2 E-key slot on this revision,
regardless of `EVK_PIN_I2S_MUX_SEL` -- and worse, enabling the mux
(`EVK_PIN_I2S_MUX_EN`/`IO8` low) while the SoC's own I2S3 TX also drives
those same pads as bus master creates direct driver contention between the
SoC and U46's outputs. (Only `I2S0_SDI`, the mic path, is oriented the
right way: `4Y` feeds the SoC's input.) Per the maintainer, this is a known
hardware issue slated for a future board rework, not something this driver
or example can route around in software.

`examples/aen/aen-evk-demo/src/main.c`'s phase 11 now compiles the I2S/PDM
playback path behind `AEN_EVKDEMO_SOUND_PLAYBACK`
(`#define`, default `0`, `examples/aen/aen-evk-demo/src/main.c:4085` ("#define AEN_EVKDEMO_SOUND_PLAYBACK 0"))
rather than deleting it -- the
maintainer intends to re-enable it once U46 is reworked, and the RX_SLEN
fix above is already the correct configuration for that reworked hardware.
With the default off: the mux `ENABLE`/`SELECT` GPIOs are never touched
(`IO8` is never driven low), `alp_i2s_open()`/`alp_audio_in_open()` are
never called, and the phase runs I2C-only -- `AMP.ENABLE` hold/release,
`tas2563_init()` on both amps, `tas2563_set_amp_level(MIN)`,
`tas2563_configure_i2s()` (register programming only, no traffic since the
bus is never started), fault-word baseline/re-read plus the raw fault pin,
`tas2563_set_mode(SHUTDOWN)`, and deinit. The printed verdict line says
which mode produced the result (`"[playback ON verdict]"` vs. `"[I2C-only
verdict, playback OFF]"`, `main.c`'s verdict block after step 13); `PASS`
in the default configuration requires the I2C checks (both amps
initialised, no shutdown-cause fault) only -- the PDM-correlation check is
additionally required only when `AEN_EVKDEMO_SOUND_PLAYBACK=1`.
`README.md`'s phase 11 row and `metadata/boards/e1m-evk.yaml`'s
`EVK_I2S_AUDIO_CODEC` doc (regenerated into
`include/alp/boards/alp_e1m_evk_routes.h`) are corrected to state the mux
cannot route audio on this revision, rather than describing U46 routing
audio to the amps as this board's normal operation.

**SAFETY, follow-up: U46 has NO Hi-Z state, so the mux-enable gate above was
not sufficient -- the i2s3 devicetree node itself now ships DISABLED on both
carrier-facing examples.** The 74LVC157 does not float its outputs when
`/E` is high; it forces them LOW. U46's `Y` outputs are `I2S0_SDO`/`SCLK`/
`WS` (the SoC's own pads, per the wiring above), so the SoC's I2S3 TX fights
U46's forced-low outputs on those pads the instant this devicetree node's
`pinctrl-0` is applied and the controller drives -- regardless of whether
`/E`/`IO8` is ever asserted. Skipping only the app-level
`i2s_configure()`/`alp_i2s_open()` call, which is what
`AEN_EVKDEMO_SOUND_PLAYBACK` did on its own, was not enough: Zephyr applies
a device's `pinctrl-0` at driver init, unconditionally, for any node left
`status = "okay"` in devicetree, whether or not application code ever opens
that device.
`examples/aen/aen-i2s-amp-alif/boards/alp_e1m_aen801_m55_he_ae822fa0e5597ls0_rtss_he.overlay`
(this example has no mux-enable GPIO of its own to hide behind, so it hit
the hazard directly) and
`examples/aen/aen-evk-demo/boards/alp_e1m_aen801_m55_he_ae822fa0e5597ls0_rtss_he.overlay`
both now ship their `i2s3` node `status = "disabled"` by default -- a
disabled node gets no `pinctrl-0` applied and no clock enabled at driver
init at all, the strongest available fix. Each example gets a
`i2s3-enable-post-u46-rework.overlay` file (`&i2s3 { status = "okay"; };`,
applied via `-DEXTRA_DTC_OVERLAY_FILE=i2s3-enable-post-u46-rework.overlay`)
as the single documented switch to turn the node back on once the board is
reworked; the demo additionally still needs `AEN_EVKDEMO_SOUND_PLAYBACK`
set to 1 -- both switches together, not either alone.
`examples/aen/aen-i2s-amp-alif/src/main.c` gains a
`DT_NODE_HAS_STATUS(I2S_NODE, okay)` compile-time branch (a disabled node's
`DEVICE_DT_GET()` fails to LINK, not just to run, so this has to be
resolved at compile time): with the node disabled, `main()` now prints why
and returns before touching the Zephyr I2S API, instead of what it used to
do -- call `DEVICE_DT_GET()` unconditionally, which was only safe because
the node was always enabled. `examples/aen/aen-evk-demo/src/main.c` needed
no equivalent change: phase 11 only ever reaches i2s3 through
`alp_i2s_open()`, and `src/backends/i2s/zephyr_drv.c`'s
`ALP_I2S_DEV_OR_NULL()` already resolves a disabled alias target to `NULL`
rather than failing to link. Both examples were rebuilt in both
configurations (node disabled/default, and node re-enabled via the new
overlay) with `-DCONFIG_COMPILER_WARNINGS_AS_ERRORS=y`; both configurations
of both examples build clean. Grepped the tree for every other `i2s3`/
`I2S0_*`/`alp-i2s0` reference (`examples/`, `tests/`, `src/`): the only
other users are `examples/audio/i2s-tone` and
`tests/unit/i2s_write_bounds`, both `native_sim`-only (`platform_allow`
excludes every AEN/EVK target, and their own file-header comments already
say `native_sim` has no real I2S controller) -- neither needs gating.

`include/alp/boards/alp_e1m_evk.h` (then `:107`) carried the same wrong
claim about the SAME PART at the time of this entry, but for the OTHER
74LVC157 pair on this board -- U38/U39, the SDIO mux (M.2 E-key SDIO vs.
the microSD slot), documented in the `:96-114` block that line sat in, not
U46/the I2S mux this entry is otherwise about. The correction still
applied: nothing distinguishes U38/U39's Hi-Z behaviour from U46's, so
`docs/cc3501e-bridge.md` (then `:518`) repeated the same wrong claim for
`SDIO_MUX_EN`. Neither file was touched by this entry; both were corrected
under #2129 (which also fixed `EVK_PIN_SDIO_MUX_SEL`'s inverted select
polarity in the same block, an unrelated bug found later). Noted here only
so the record is straight; nothing on this branch repeats that claim for
either mux pair.

**Review follow-up: gate the phase on BOTH amps, not "at least one."**
`phase_sound()`'s only bring-up gate was `ok_amps == 0` (`main.c`, step 4)
-- with a single amp answering, the phase printed `1/2 amp(s) up` and still
returned `PHASE_PASS`, contradicting the phase header and the README row,
both of which say PASS requires both amps. This mattered more once playback
went off by default, since with `AEN_EVKDEMO_SOUND_PLAYBACK=0` the I2C
bring-up of both amps IS the whole phase. Fixed by gating on
`ok_amps != (int)AMP_COUNT` (chosen over reworking the docs to "at least
one"; the demo's job is to show both U27 and U28 answering, and a fitted
board where only one does is a real, reportable fault) right after the
`new_shutdown_fault` check, so both playback-off and playback-on runs
require the full 2/2 before considering the phase a pass.

**Documentation-only corrections**, each citing the exact section rather
than the loose earlier claim, none change behaviour:
- Multi-byte writes: SLASET3D §7.3.5 "Single-Byte and Multiple-Byte
  Transfers" (p.32) DOES document sequential-address auto-increment in
  prose; §7.3.7 (p.33), a different, later section, merely doesn't restate
  it. `tas2563_load_tuning()`'s doc now cites §7.3.5 and frames single-byte
  writes as deliberate conservatism pending silicon, not an undocumented gap
  (`include/alp/chips/tas2563.h:909-917` ("§7.3.5")). The same imprecise framing in the
  still-unreleased `changelog.d/2040-tas2563-v03x-tuning-i2s-faults.md` is
  corrected in place rather than left to ship self-contradicting notes —
  this does not close any part of #2040 otherwise.
- `tas2563_set_amp_level()`: SLAA953 §1.9's full note — "Amplifier Level
  cannot be changed in Smart Amp/Tuning Mode. In order to change it, user
  must enter ROM mode in Test and Measurement panel in the Device Home
  page" — is a PPC3-GUI workflow note, not a register rule; SLASET3D itself
  never mentions ROM mode. No register in SLASET3D's map selects or reports
  ROM vs Smart Amp/Tuning mode (searched exhaustively); documented, not
  gated in code (`include/alp/chips/tas2563.h:555-573` ("SLAA953 §1.9")).
- `tas2563_load_tuning()`: TI's `I2C_CKSUM` (0x7E) write-verification CRC
  (SLASET3D §7.3.2, p.29, not just SLAA765's command-syntax examples) is
  documented but its algorithm is never specified — not what it sums, not
  whether `PAGE`/`BOOK` writes count, not an initial value beyond POR `0h` —
  so a host-side expected value cannot be computed, only guessed; not
  implemented (`include/alp/chips/tas2563.h:923-938` ("I2C_CKSUM")).
- Calling `tas2563_init()` again now visibly wipes any loaded tuning/I2S/
  IV-sense configuration (the software reset above is unconditional);
  documented on both `tas2563_init()` and `tas2563_load_tuning()`.

**Tests** (`tests/zephyr/chips/src/test_audio.c`): SW_RESET write-ordering
(`:358`), SW_RESET settle wait with `sd_n == NULL` (`:407`, 1x floor), the
sd_n-owned settle wait in `test_tas2563_init_settles_sdz_before_first_access_when_sd_n_owned`
(`tests/zephyr/chips/src/test_audio.c:1094`
("test_tas2563_init_settles_sdz_before_first_access_when_sd_n_owned")),
tightened to 2x — one wait alone no longer passes), the NACK-propagation
test (`:438`), and a new `test_tas2563_init_writes_sd_n_before_configuring_it`
(`tests/zephyr/chips/src/test_audio.c:1179`
("test_tas2563_init_writes_sd_n_before_configuring_it")) for the SD_N
glitch fix. `test_tas2563_configure_i2s_writes_tdm_fields` (`:497`) already
covered `TDM_CFG2` bits `[5:4]` = `01b`/`10b` for `TAS2563_RX_LEFT`/`_RIGHT`
against the fake (full-byte assertions with the bit breakdown spelled out in
each case's comment), so no new coverage was needed for the slot-mapping fix
below. `fake_tas2563.c` now models `SW_RESET` as a real reset (register
defaults + book/page reload, test-only write log/counts left untouched) so
`test_tas2563_init_parks_amp_in_software_shutdown` (`:246`) and
`test_tas2563_init_selects_book0_not_just_page0` (`:324`) observe the actual
mechanism instead of coincidentally passing via the now-redundant park
write; both were updated for the new expected value (`0x0Eu`).

**Close-before-assert, precisely scoped.** The shared `tas_init()` helper
(`:224`), which roughly 15 tests call, now closes the bus on its own
init-failure path before asserting, fixing the leak for every one of its
callers on that path. Two tests needed a SECOND fix beyond that: `:246` and
`:324` both still had a later assertion, reading `fake_tas2563_get_reg()`/
`fake_tas2563_cur_book()` (not the bus handle), positioned before their
final `alp_i2c_close(bus)` -- a failure there, after `tas2563_init()` had
already succeeded, would still have leaked. Both now close right after
`tas2563_init()` returns, before any later assertion, since nothing either
test does afterward touches the bus handle itself. The four newly-added
tests follow the same pattern from the start.

**The SD_N-glitch test needed two attempts.** `gpio_emul_pin_configure()`
fires every registered callback synchronously on every `configure()` call,
even with no interrupt armed (upstream Zephyr's `drivers/gpio/gpio_emul.c`,
not vendored in this tree, `gpio_emul_pin_configure()`) — the first version
of the new test used that to capture `gpio_emul_output_get()` at the
instant `tas2563_init()`'s own `configure()` ran, expecting the
pre-configure write to have already landed. It passed against BOTH the
fixed driver and a configure-then-write-once mutation, because
`gpio_emul_port_set_bits_raw()` (the write path) ALSO fires the same
callback on every successful write ("for output-wiring, so the user can
take action based on output," the driver's own comment on that call) — so
the capture-only-the-latest
approach was overwritten by the trailing write's own (always-correct)
callback regardless of ordering. The test now logs EVERY firing, in order,
and requires all of them to read high, not just the last one — see the
test's own comment for the full reasoning.

Mutation-checked: reverting the status-propagation fix, deleting the whole
software-reset block, isolating either settle wait, and swapping SD_N's
write order back to configure-then-write-once were each tried. All turned
exactly their target test(s) red; each was then restored and the suite
re-confirmed green. The write-order mutation specifically: 282/283 passed,
with `test_tas2563_init_writes_sd_n_before_configuring_it` failing on
"SD_N read 0 at configure/write event #0" — the FIRST logged event being
low is exactly what identifies the bug.

**Silicon-open, recorded rather than guessed at:** whether the `SW_RESET`
write itself ACKs (a NACK would surface as `ALP_ERR_IO` on every boot);
whether 200 us is enough on real parts; whether a 24 ms SD_N low pulse
actually reaches Hardware Shutdown; P5_2's data-register level at first
configure on a real EVK; and what the CC3501E proxy's `configure()` does to
output level. Nothing in this entry has been run on silicon.
