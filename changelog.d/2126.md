### Added — auto-recover a wedged CC3501E bridge link (#2126)

Before, a bridge wedge (observed ~3 times per 50 Wi-Fi connects on
e1m-aen-evk-01) left every request failing (`ver` → `-4`/`-5`) until the
whole board was power-cycled — the host-side warm-reset fix
(`cc3501e_recover()`) existed but nothing called it automatically.

Now `cc3501e_link_check_and_recover()` is wired into the driver's own
worker-routed failure exits and into `cc3501e_wifi_connect()`'s timeout
exit: when a top-level op returns `ALP_ERR_IO`/`ALP_ERR_TIMEOUT` with no
reply ever decoded off the wire, it probes with a few `PING`s and, only if
every one fails, warm-resets and re-confirms the link — guarded against an
open OTA/update session, a 30 s cooldown between recoveries, and
`CONFIG_ALP_SDK_CC3501E_AUTO_RECOVER` (default `y`). `alp companion
recover` exposes the same warm reset unconditionally for manual use.
