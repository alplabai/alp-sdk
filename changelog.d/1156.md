### Investigated

- `metadata/pinmux/v2n.yaml`'s `e1m_pad: "TBD"` on all 207 rows (and
  `e1m_function: "TBD"` on the 145 Renesas-owned rows) is confirmed real, not
  a bug: the E1M-connector edge-ball assignment for RZ/V2N does not exist
  yet, in this repo or the internal metadata repository. Filling it in
  requires a SoM netlist export in the same shape as AEN's `from-alif.tsv`,
  which has not been produced. Left as-is pending that export (#1156).
