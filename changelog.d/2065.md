### Fixed — the E1M-EVK rotary encoder part number named the wrong Bourns series (#2065)

`metadata/boards/e1m-evk.yaml:111` and `:258`, `metadata/templates/catalog-v1.json:87`
(and the generated `include/alp/boards/alp_e1m_evk_routes.h` they feed) named
the E1M-EVK's rotary encoder `PEC12R-4222F-S0024`. The 2626-R2 EVK schematic
netlist (`E1M-EVK-2626-R2_components.csv`, git-tracked in `alp-sdk-internal`
at `metadata/boards/e1m-evk/netlists/`, corroborated by a matching raw Altium
export) names designator `E1` as `PEC11R-4215K-S0024`, "Incremental encoder
24 detents 24 pulses", `Component Kind=Standard`. `PEC11R` and `PEC12R` are
different Bourns series; both option codes share the `S0024` (24 PPR) suffix
but differ in detent count, torque, shaft length/style, bushing, and switch
presence/debounce. The netlist export used here carries no DNP/fitted
column (every part reads `Standard`, `Standard (No BOM)`, or `Net Tie`), so
it establishes as-designed intent only, not as-assembled -- corroborated by
the separately git-tracked `E1M-EVK-2626-R2_components.csv` revision, which
does carry a DNP column and marks `E1` `DNP=0` (fitted) in the Alif variant.

`metadata/boards/e1m-x-evk.yaml` (`:60`, `:148`, `:247`) names `PEC12R-4222F`
for the **E1M-X EVK**, a different carrier board on a different netlist.
Left unchanged: no netlist evidence has been reviewed for that board in this
change, so its encoder part number stays **unverified**, not confirmed
different from the E1M-EVK's -- there is simply no netlist-backed claim
either way for the E1M-X EVK yet.

`include/alp/boards/alp_e1m_evk_routes.h` regenerated via
`scripts/gen_board_header.py`; `tests/fixtures/emit-snapshots/proj-aen.
composed-route-table.snap` and `.../proj-aen.carrier-netlist.snap`
refreshed via `scripts/check_emit_snapshots.py --update` to match, byte-shape
unchanged apart from the corrected doc string.

**Full-tree sweep** (`git grep -n 'PEC12R\|PEC11R\|4222F\|4215K'`): every
other hand-authored E1M-EVK mention of the stale `PEC12R-4222F-S0024` string
is corrected in this same change -- `docs/boards/e1m-evk.md` (:126, :183),
`docs/soms/aen.md` (:302), `examples/aen/aen-evk-demo/boards/
alp_e1m_aen801_m55_he_ae822fa0e5597ls0_rtss_he.overlay` (:235),
`examples/aen/aen-qenc-readout/README.md` (its own note about the mismatch
this issue fixes, now updated to say RESOLVED),
`examples/aen/aen-qenc-readout/boards/
alp_e1m_aen801_m55_he_ae822fa0e5597ls0_rtss_he.overlay` (:46, a direct quote
of the old `metadata/boards/e1m-evk.yaml` doc string), `examples/aen/
aen-qenc-readout/src/main.c` (:169, "the PEC12R's A/B contacts" ->
"the PEC11R's"), `examples/peripheral-io/gpio-button-led/board.yaml` (:45),
`examples/peripheral-io/qenc-readout/board.yaml` (:19) and its `src/main.c`
(:20, the E1M EVK half of a both-EVKs comment -- the E1M-X EVK half at :21
is annotated unverified per above), `scripts/alp_template.py`'s docstring
(:846, illustrating the real doc text), and `tests/scripts/
test_alp_template.py` (three literals mirroring the old real text kept in
sync, plus one assertion re-targeted from the now-meaningless
`PEC12R-4222F-S0024 not in ...` to `PEC11R-4215K-S0024 not in ...` so it
still actually exercises the doc-substitution guard it names).

**Left unchanged, and why:** `CHANGELOG.md:16572` and `:22393` are dated,
released history under `## [v0.6.0] - 2026-06-06` -- they record what the
v0.6.0 PR actually said and shipped as `PEC12R-4222F-S0024` at the time;
`changelog.d/README.md` treats a released section as history to correct
only for a factual typo about *that PR*, not to retrofit later findings, so
these stay as the dated record of what was believed then.
`metadata/boards/e1m-x-evk.yaml`, `include/alp/boards/
alp_e1m_x_evk_routes.h`, and every `tests/fixtures/emit-snapshots/proj-v2n.*`
/ `scaffold.peripheral-v2n101.snap` doc string are the **E1M-X EVK's own**
`PEC12R-4222F` -- unrelated part, unrelated carrier, unverified either way,
left exactly as they are.
