### Documented — governance coverage gaps closed and ADR bookkeeping corrected (#2245)

The whole-repo doc audit found the release/audit-gating governance docs and
several ADRs describing a state the tree no longer matches. Corrections, each
verified against code, tests, and workflows:

`docs/abi-markers.md` gains a "Subdirectory + extension headers" section so
its "every public header is classified" claim holds: the generated board
routes headers are `[ABI-STABLE]`, `boards/alp_e1m_x_evk.h` and the vendor
`ext/` escape hatches are `[ABI-EXPERIMENTAL]`, and the marker-less facades
(`boards/alp_e1m_evk.h`, `blocks/*`, `protocol/cc3501e.h`,
`protocol/crc16.h`) are named. `docs/threat-model.md`'s per-surface catalogue
gains the missing rows — notably `<alp/model.h>` (the `.alpmodel` CBOR
manifest is the untrusted-parse surface the catalogue existed for), plus
`jpeg.h`, `i3c.h`, `usb.h`, `display.h`/`gui.h`, `ahrs.h`/`pid.h`/`tmu.h`,
and n/a rows for the constants-only headers.

ADR bookkeeping: 0020's Status now records the 0022 supersession and the 0026
amendment, and its oracle-corpus claim is corrected to the five fixtures
actually under `tests/parity/oracle/` (`multicore_rpmsg-imx93` was deleted,
per `ORACLE-PROVENANCE.txt`); 0010's Status cross-references 0022's Renode CI
deletion; 0033 records the implemented bilingual MAJOR-3 migration gate
(`ALP_CC3501E_PROTOCOL_MAJOR_LEGACY`) its decision text omitted, points the
current-version claim at the ledger (4.0), and date-stamps the drifted code
citation; 0002 no longer cites a gpio test that exists only on
`origin/integration/1494-1734-back`; 0006's v0.4 roadmap table is marked
historical with the undelivered rows named.

Other: `docs/ota.md` cites ADR 0031 (superseding 0015) for the CC3501E
firmware location and notes modules ship factory-flashed with the latest
firmware; `CHANGELOG.md`'s dangling `alp_uptime_ms()` pointer now points at
the pending `changelog.d/1953.md` fragment; `docs/test-plan.md`'s conformance
row counts the actual 16 classes × 8 cases; `docs/v1.0-readiness.md`'s
"measured now" example count is 169 main.c files and its 30+49=79 arithmetic
slip is annotated.
