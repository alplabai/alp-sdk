### Fixed — ADR-0021/0020 amendments corrected for cleared tan blockers; ADR-0003's Status line records the ADR-0023 supersession (#1516)

`docs/adr/0021-toolchain-provisioning.md`'s newest amendment (2026-08-07) named
a Lane-1 P1 blocker -- `build_cmd.py`'s hardcoded `toolchain_root=None`, and
silent `--materialise` demotions -- that is closed as of tan-cli `v0.6.0-rc1`
(`resolve_toolchain_root()` wired at `build_cmd.py:1497`; `_demoted_artefact_issues()`
at `:1406`/`:1545`). A new dated amendment records that, per this ADR's own
append-only convention.

`docs/adr/0020-sdk-owns-build-execution.md`'s 2026-08-12 amendment said PR
`alplabai/tan-cli#530` was "not yet in a released `tan`", with the latest
release at `v0.5.1`. It shipped in `v0.6.0-rc1` (tagged 2026-08-14, carried
into the final `v0.6.0`, tagged 2026-08-24) -- a new dated amendment corrects
this without editing the 2026-08-12 text in place.

`docs/adr/0003-peripheral-coverage.md`'s bare `Status: Accepted` gave no
pointer to `docs/adr/README.md`'s own record that its Ethernet row is
contested by ADR-0023 (Proposed) -- the only supersession in the index whose
Status line omitted the pointer the other two superseded ADRs (0014, 0006)
carry. Fixed to match that precedent.
