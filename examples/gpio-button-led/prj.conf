# SPDX-License-Identifier: Apache-2.0
#
# This example is fully board.yaml-driven -- every CONFIG_* is
# derived from board.yaml by scripts/alp_project.py (invoked from
# CMakeLists.txt at configure time, output at
# build/generated/alp.conf).  Do not hand-edit; if board.yaml
# can't express what you need, file an issue to extend the schema.

rsource "build/generated/alp.conf"
