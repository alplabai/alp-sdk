# board.yaml -- per-app project configuration for v2n-ethernet-dual.
schemaVersion: 2
som:
  sku: E1M-V2N101

preset: e1m-x-evk
cores:
  a55_cluster:
    os: "off"
  m33_sm:
    app: ./src
    peripherals: [ethernet]

chips:
  - rtl8211fdi

diagnostics:
  log_level: info
