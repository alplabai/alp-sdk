# board.yaml -- per-app project configuration for v2n-ethernet-dual.

schema_version: 2

som:
  sku: E1M-V2N101

carrier:
  name: E1M-X-EVK

cores:
  a55_cluster:
    os: "off"
  m33_sm:
    app: ./src
    peripherals: [ethernet]

chips:
  - rtl8211fdi

diagnostics:
  log_level: info
