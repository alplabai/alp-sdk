CONFIG_PRINTK=y
CONFIG_LOG=y

# Zephyr flash subsystem -- the example calls flash_read /
# flash_erase / flash_write against the on-module xSPI NOR via the
# DT alias `xspi-flash` (set in the V2N board overlay).
CONFIG_FLASH=y
CONFIG_FLASH_PAGE_LAYOUT=y
