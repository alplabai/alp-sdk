# SPDX-License-Identifier: Apache-2.0
#
# Intentionally empty.  Every CONFIG_* this app needs is derived
# from board.yaml by scripts/alp_project.py and layered in via
# EXTRA_CONF_FILE (see CMakeLists.txt).  Add app-specific tuning
# knobs here only when they are NOT feature-selection.

# App-specific stack tuning -- the v0.2 inference pipeline lands
# Vela-compiled model buffers on `main`'s frame, so keep main
# generous.  Not a feature-select knob, hence not in board.yaml.
CONFIG_MAIN_STACK_SIZE=4096
