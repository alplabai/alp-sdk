# SPDX-License-Identifier: Apache-2.0
#
# Bench config for the LPTIMER counter RAM-run on the E1M-AEN801 (Alif Ensemble
# E8).

# Console: Alp UART console -- E1M edge UART0 (Alif UART5, P3_4/P3_5, 115200
# 8N1, NS16550), the E1M-AEN801 carrier console. The board _defconfig already
# selects it; we set it explicitly here so the example is self-describing.
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_UART_INTERRUPT_DRIVEN=y
# Serial-less bench? Comment the four lines above and uncomment these to read
# 'ram_console_buf' over SWD instead:
# CONFIG_RAM_CONSOLE=y
# CONFIG_RAM_CONSOLE_BUFFER_SIZE=2048
# CONFIG_UART_CONSOLE=n
CONFIG_PRINTK=y

# --- LPTIMER counter ---------------------------------------------------------
# CONFIG_COUNTER_ALIF_LPTIMER (the Tier-1.5 driver) is `default y` under `if
# ALP_SDK` once the okay'd "alif,lptimer" node (lptimer@42001000) exists, so
# ALP_SDK MUST be on to surface it; COUNTER pulls in the Zephyr counter
# subsystem the driver registers against.
CONFIG_ALP_SDK=y
CONFIG_COUNTER=y

# Keep the image small + deterministic for a RAM-run.
CONFIG_LOG=n
