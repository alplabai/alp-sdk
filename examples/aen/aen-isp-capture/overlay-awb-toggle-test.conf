# SPDX-License-Identifier: Apache-2.0
#
# Bench run 83, decisive test image D only (layer on top of
# overlay-ae-diag.conf, not instead of it): enables src/main.c's live
# AUTO_WHITE_BALANCE=0/1 toggle at frames 3/6, exercising isp_pico.c's
# isp_set_ctrl() live-apply path (only reachable while already streaming)
# instead of the stream-start apply every other image in this directory
# uses. See examples/aen/aen-isp-capture/Kconfig for what this
# actually does.
CONFIG_AEN_ISP_AWB_LIVE_TOGGLE_TEST=y
