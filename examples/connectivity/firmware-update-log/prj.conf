CONFIG_PRINTK=y
CONFIG_LOG=y
CONFIG_ALP_SDK=y
CONFIG_ALP_SDK_UPDATE_LOG=y

# The update-log engine hashes records on the caller's stack. Keep enough
# main-thread stack for Cortex-M builds with hardware stack protection enabled.
CONFIG_MAIN_STACK_SIZE=4096
