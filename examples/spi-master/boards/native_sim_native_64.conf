# SPDX-License-Identifier: Apache-2.0
#
# native_sim has no real SPI controller; pull in the emul SPI
# driver so the bus enumerates at boot.  Auto-loaded by Zephyr
# when building for native_sim/native/64.

CONFIG_EMUL=y
CONFIG_SPI_EMUL=y
