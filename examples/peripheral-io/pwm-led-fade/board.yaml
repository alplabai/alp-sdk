# board.yaml -- per-app project configuration for the
# pwm-led-fade example.
#
# Customer workflow: `tan init --from-example peripheral-io/pwm-led-fade`, change `som.sku` to
# your MPN, edit the `preset:` (or use an inline board) if you're on a non-EVK board,
# and `tan build`.  CMakeLists.txt invokes
# scripts/alp_project.py at configure time + layers the
# generated alp.conf over prj.conf via EXTRA_CONF_FILE.
som:
  sku: E1M-AEN801

preset: e1m-evk
supported_boards:
  - e1m-evk
  - e1m-x-evk

# E1M pads this app uses (subset of the EVK preset's `e1m_routes:`).
pins:
  - { e1m: E1M_PWM3, macro: EVK_PWM_LED_GREEN, doc: "RGB LED green channel; schematic-wired via PWM3 -- the channel this fade demo drives. Bench-measured 2026-07-28: PWM3 (not PWM0) is green, the netlist's LED_R/LED_G net names are swapped" }
cores:
  m55_hp:
    app: ./src
    # Zephyr peripheral subsystem exercised by this example.  The
    # loader translates the entry to CONFIG_<X>=y in alp.conf.
    peripherals:
      - pwm

diagnostics:
  log_level: info
