# board.yaml -- per-app project configuration for the
# spi-slave example.
#
# SPI target (slave) mode via the portable alp_spi_target_*
# surface in <alp/peripheral.h> (v0.9): transfer-based
# request/response protocol clocked by an external controller.
# Needs controller-driver slave support at runtime; the example
# degrades cleanly (diagnostic + exit) where none exists -- e.g.
# every native_sim build.
#
# Customer workflow: copy this directory, change `som.sku` to
# your MPN, edit the `preset:` (or use an inline board) if you're on a non-EVK board,
# and `west build`.  The planner (scripts/alp_orchestrate) derives the
# per-core Kconfig from this file and wires it into `west build` via
# `-DEXTRA_CONF_FILE=...` (see docs/adr/0020-sdk-owns-build-execution.md).
som:
  sku: E1M-AEN801

preset: e1m-evk
supported_boards:
  - e1m-evk
  - e1m-x-evk

# E1M pads this app uses (subset of the EVK preset's `e1m_routes:`).
pins:
  - { e1m: E1M_SPI1, macro: EVK_SPI_BUS_ARDUINO, doc: "Arduino UNO header SPI; terminates on the on-module CC3501E, not the main SoC" }
cores:
  m55_hp:
    app: ./src
    # Zephyr peripheral subsystem exercised by this example.  The
    # loader translates the entry to CONFIG_SPI=y in alp.conf.
    peripherals:
      - spi

diagnostics:
  log_level: info
