CONFIG_ZTEST=y

# Real Zephyr shell on the dummy backend so the console TU's own
# shell_print() output is capturable, exactly like tests/console does.
CONFIG_SHELL=y
CONFIG_SHELL_BACKEND_SERIAL=n
CONFIG_SHELL_BACKEND_DUMMY=y
CONFIG_SHELL_BACKEND_DUMMY_BUF_SIZE=2048
CONFIG_LOG=n
