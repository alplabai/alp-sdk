# SPDX-License-Identifier: Apache-2.0
#
# E1M-AEN801 (Alif Ensemble E8) RTSS-HP: pin the SoC capability
# profile so the conformance expectations derive from alp_has()
# (strict caps<->backend parity) instead of the permissive
# CONFIG_ALP_SOC_NONE fallback.  SoC per the board's board.yml
# (ae822fa0e5597ls0 = Ensemble E8).
CONFIG_ALP_SOC_ALIF_ENSEMBLE_E8=y
