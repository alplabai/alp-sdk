# SPDX-License-Identifier: Apache-2.0
#
# Overlay onto prj.conf for the alp_sdk.peripheral.caps_e3 scenario.
# Pins the SoC choice to Alif Ensemble E3 so the capability-validation
# tests in main.c can verify *_open rejects configs that exceed the
# E3's documented hardware caps.

CONFIG_ALP_SOC_ALIF_ENSEMBLE_E3=y
