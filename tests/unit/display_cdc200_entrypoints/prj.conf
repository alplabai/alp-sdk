CONFIG_ZTEST=y
CONFIG_DISPLAY=y
# native_sim pulls the SDL display driver in with CONFIG_DISPLAY; this suite
# needs the class symbol only, and SDL is absent on the plain native_sim/native
# variant this testcase.yaml also allows (SDL2/_real_SDL_config.h: not found).
CONFIG_SDL_DISPLAY=n
