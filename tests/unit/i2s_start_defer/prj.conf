# SPDX-License-Identifier: Apache-2.0
#
# CONFIG_ALP_SDK + a pinned SoC ref + CONFIG_I2S link the real
# src/backends/i2s/zephyr_drv.c against the fake alp-i2s0 device the
# boards overlays in this test supply -- mirrors
# tests/unit/i2s_write_bounds' prj.conf (issue #1619).
CONFIG_ZTEST=y
CONFIG_ALP_SDK=y
CONFIG_ALP_SOC_ALIF_ENSEMBLE_E7=y
CONFIG_I2S=y
CONFIG_HEAP_MEM_POOL_SIZE=4096
