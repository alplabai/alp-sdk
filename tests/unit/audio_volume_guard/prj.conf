# CONFIG_ALP_SDK + a pinned SoC ref link the real audio backend
# registry (audio_dispatch.c + zephyr_drv.c + sw_fallback.c) so the
# real-entry-point tests below can fetch sw_fallback's registered
# out_set_volume via alp_backend_select()/alp_backend_select_next()
# -- mirrors tests/unit/audio_registry's prj.conf (issue #2002).
CONFIG_ZTEST=y
CONFIG_ALP_SDK=y
CONFIG_ALP_SOC_ALIF_ENSEMBLE_E7=y
