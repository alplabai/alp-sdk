som:
  sku: E1M-AEN801    # Alif Ensemble E8 -- metadata/socs/alif/ensemble/e8.json
preset: e1m-evk      # carries no 'emmc'/'emmc_lp' key at all
cores:
  m55_hp:
    app: ./src
    peripherals:
      - emmc          # triggers ALP-B010: emmc is board-side (I/O controller), not a direct E8 SoC cap

