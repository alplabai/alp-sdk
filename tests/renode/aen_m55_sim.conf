# SPDX-License-Identifier: Apache-2.0
# Kconfig fragment for the AEN M55 --sim-mode Renode gate (#687).
# The AEN M55-HP has a wired UART5 console (board default), so no
# ram_console is needed -- the sim streams UART5 over a socket terminal.
CONFIG_BOOT_BANNER=y
CONFIG_PRINTK=y
