@PACKAGE_INIT@

# AlpSdkConfig — find_package(AlpSdk CONFIG REQUIRED)
#
# Imports targets:
#   alp::sdk_headers  — interface library exposing <alp/...>
#   alp::sdk          — static library with the selected OS backend

include("${CMAKE_CURRENT_LIST_DIR}/AlpSdkTargets.cmake" OPTIONAL)

check_required_components(AlpSdk)
