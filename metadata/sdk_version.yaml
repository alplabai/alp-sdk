# Single source of truth for the Alp SDK release version.
#
# Read by:
#   - scripts/alp_project.py        -- compared against the family /
#                                       board hw_revisions tables
#                                       when emitting any backend
#                                       config; refuses to emit when
#                                       the requested hw_rev is
#                                       outside [min_sdk_version,
#                                       max_sdk_version].
#   - scripts/validate_board_yaml.py -- same check, exit code 3 on
#                                       mismatch.
#   - alplabai/alp-sdk-vscode        -- surfaces an incompat warning
#                                       in the configurator panel
#                                       (extension lives in its own
#                                       repo since 2026-05-12).
#
# Bump in the same PR that lands the matching CHANGELOG.md entry.

version: 0.8.0
status:  released        # released | development | preview
