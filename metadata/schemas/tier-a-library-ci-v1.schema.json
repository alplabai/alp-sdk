{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alplab.example/schemas/tier-a-library-ci-v1.schema.json",
  "title": "ADR 0018 Tier-A library CI registry",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "hostBuild", "familyMatrix"],
  "properties": {
    "schemaVersion": {
      "const": "tier-a-library-ci-v1"
    },
    "_comment": {
      "type": "string"
    },
    "hostBuild": {
      "type": "object",
      "additionalProperties": false,
      "required": ["platform", "libraries", "excludedLibraries"],
      "properties": {
        "platform": {
          "type": "string",
          "minLength": 1
        },
        "libraries": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          }
        },
        "excludedLibraries": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "familyMatrix": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["family", "som", "core"],
        "properties": {
          "family": {
            "type": "string",
            "minLength": 1
          },
          "som": {
            "type": "string",
            "pattern": "^E1M-[A-Z0-9]+$"
          },
          "core": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "excludedFamilies": {
      "type": "object",
      "description": "familyMatrix `family` values that stay listed (so scripts/validate_metadata.py still sees every SoM family covered) but are skipped by scripts/gen_tier_a_ci_matrix.py when deriving the actual CI build matrix, each with a required reason -- same shape as hostBuild.excludedLibraries.",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    }
  }
}
