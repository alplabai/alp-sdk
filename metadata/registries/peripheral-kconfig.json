{
  "schemaVersion": "peripheral-kconfig-v1",
  "_comment": "Single source for board.yaml `peripherals:` token -> Zephyr subsystem Kconfig symbol. Consumed by scripts/alp_project.py and scripts/alp_orchestrate/slugs.py so per-slice config emitters do not duplicate this table.",
  "peripherals": {
    "adc": "ADC",
    "can": "CAN",
    "counter": "COUNTER",
    "gpio": "GPIO",
    "i2c": "I2C",
    "i2s": "I2S",
    "pwm": "PWM",
    "rtc": "RTC",
    "sensor": "SENSOR",
    "spi": "SPI",
    "uart": "SERIAL",
    "watchdog": "WATCHDOG"
  }
}
